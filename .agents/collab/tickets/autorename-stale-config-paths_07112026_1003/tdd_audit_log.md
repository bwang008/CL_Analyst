2026-07-11 10:22:42 | autorename-stale-config-paths_07112026_1003 | TDD-TESTER | Wrote tests/test_autostamp_config_rewrite.py (Strategy A: real powershell.exe over tmp_path fixtures for the extracted rewrite .Replace + BOM-less write, plus real-PS execution of the shipped rewrite region for the two warning branches; plus text-scan fix-pinning of the two else branches; plus a pure-Python logic-guard mirror). 12 tests; confirmed 4 fix-pinning tests are RED against pristine gcp/run_sweep_batch.ps1 and all 12 GREEN against a simulated fix. Script left byte-identical to HEAD; no reports/ dirs touched.
[2026-07-11 10:31:56] | autorename-stale-config-paths_07112026_1003 | TDD-CODER | Added two additive Yellow Write-Host WARNING else branches in gcp/run_sweep_batch.ps1 (else on `if (Test-Path $stampedCfgDir)` naming missing dir + predictions_path not rewritten; else on `if ($cfgPatched -gt 0)` stating 0 config(s) matched/were rewritten); .Replace and BOM-less write untouched; 12 passed.
