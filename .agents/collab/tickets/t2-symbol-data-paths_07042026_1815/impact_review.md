# Impact Review — T2 Data-path + contract symbol propagation
**Ticket:** `t2-symbol-data-paths_07042026_1815`
**Reviewer:** Ticket-Impact-Reviewer | **Date:** 2026-07-04 | **Branch:** `development` @ `fe1ce5e`
**Proposal reviewed:** `audit.md` (Ticket-Auditor, 2026-07-04), with manager rulings treated as given (MCL brain=CL ACK; `ibkr_execution.py:73` 1-liner included; ~16 mechanical test additions approved; niceties deferred to T6).

## VERDICT: REQUIRE HUMAN AUTHORIZATION
**Technical review: PASS (conditional).** Every riskiest claim survived independent verification (details §2); the design is the correct localized shape and the CL byte-identity story is real and pinned by regression tests. The halt is procedural, not technical: the proposal is a **multi-component refactor** (Refactor Veto triggered — §1.3), and my workflow strictly forbids autonomous approval of such regardless of justification quality. **On human ACK, this review converts to APPROVE with conditions C1–C8 (§3) — no re-review round needed.**

Recommendation to the human: **authorize.** The multi-component scope is not auditor scope-inflation — it is exactly the file set the gap-analysis blueprint's T2 sketch enumerates (`ibkr_client.py`, `adapters/ibkr_data_feed.py`, `data_manager.py`, `live_trader.py:329-381`, `cli.py:167-172`), and B1/B2/B3 are structurally unfixable with fewer components (the CL assumption lives in all five layers simultaneously; fixing fewer leaves silent CL-misdata paths alive).

---

## 1. Constraint evaluation (workflow rules)

### 1.1 Interface Rule — TRIGGERED, justification ACCEPTED
Four signature-level changes, each verified against the full caller census:
- **Manager fetch methods gain required kw-only `symbol`** (`ibkr_client.py:255/:715/:766`). All three are already keyword-only (`*` in signature) — clean addition; any missed caller fails loudly with TypeError, never silently-CL. Callers: adapter pass-throughs ×3 + module convenience `:1294-1315` (kept CL-bound, passes `symbol="CL"` explicitly) — all enumerated in the proposal. Nothing else calls them (repo grep).
- **`IBKRDataFeedClient.__init__` gains required `instrument_context`.** Verified: the adapter is constructed in exactly ONE place repo-wide — `factories.py:9` via `**kwargs` pass-through from `cli.py:289`. Zero test or script constructions exist. Blast radius = one call-site edit in cli.
- **`DataManager.__init__` gains required kw-only `symbol`.** Already keyword-only. Production sites: exactly 2 (`live_trader.py:331`, `:376`). Test churn quantified in §2 V5 (census corrections found, same conclusion).
- **`data_feed_interface.py:83` drops the `= "CL"` default on `get_front_month_contract`.** Verified safe: every caller passes symbol explicitly (`live_trader.py:629-631`, `:2112-2114`; adapter-internal `:98`/`:140`; `ibkr_execution.py:72`). Note: this makes the summary phrase "interface byte-unchanged" imprecise — the precise claim (audit D1) is that the **fetch-method block `:19-52` and `SimulatedDataFeed`** are byte-unchanged, which is true (see C6).

Justification is strong and alternatives were genuinely weighed (D1: a per-call `symbol="CL"` default re-creates the silent-CL trap the ticket kills — violates the house no-silent-defaults rule; a required per-call param churns interface + sim + 5 call sites for zero information since all fetches are brain-continuous — verified, §2 V2). Accepted.

### 1.2 Base Class Rule — TRIGGERED (marginal), justification ACCEPTED
The abstract `DataFeedClient` changes by exactly one default-value removal; `SimulatedDataFeed` (the only other implementation) is untouched — Python does not enforce abstract signature parity, and the sim's own `= "CL"` default at `simulated_data_feed.py:205` remains valid. `DataManager` is a core component but not inherited. Accepted.

### 1.3 Refactor Veto — TRIGGERED → Mandatory Human Authorization Guardrail
This fix rewires more than one component: (1) `ibkr_client.py` contract-construction layer (new builder + 3 fetch signatures + 2 front-month exchange sources), (2) `adapters/ibkr_data_feed.py` (constructor + routing), (3) `data_manager.py` (new path authority, constructor, module-global deletion), (4) `live_trader.py` construction block + subscription symbols, (5) `cli.py` default resolution + merge gate, plus a 1-line interface edit and a 1-line exec-adapter edit. Five components substantively modified = multi-component refactor. Per the guardrail I am **strictly forbidden** from autonomously approving and must route to the HUMAN USER via the Ticket-Manager. The Auditor's justification (relayed for the human): the live engine is CL-shaped at every layer below the symbol string (blueprint B1/B2/B3, all BLOCKER-severity silent-misdata vectors); localized alternatives were considered and rejected in audit D1/§8.1-3; CL behavior is pinned byte-identical by regression tests 16/22/27 plus a mandatory HS14B parity-gate re-run.

---

## 2. Independent verification of riskiest claims

| # | Claim | Result |
|---|---|---|
| V1 | **D4 CL byte-identity** — all 7 CL paths equal today's literals | **CONFIRMED.** `data_manager.py:52-59`: `get_data_path("raw/cl-5m_bk.csv")`, `get_data_root()/processed/warm_start_cache.parquet`, `.../cl_continuous_master.parquet`, `.../.roll_metadata.json`. `live_trader.py:139-142` (same 5m seed/cache), `:334` (`cl_continuous_master.parquet`), `:346` (`warm_start_cache_1h.parquet`), `:357` (`CL_raw_1h.parquet`), `:379` (`cl_continuous_master_1h.parquet`). `cli.py:167-172` imports live_trader's constants (identical values, imports at `:108-109`). Exception census exact: 3 of 7 (5m cache, 1h cache, roll metadata) deviate from the generic patterns; the other 4 already match `{sym}`/`{SYM}` patterns. The two shipped `seed_path_1h` config overrides (HS13A `:17`, HS11-E01 `:23`, both `processed/CL_raw_1h.parquet`) are preserved verbatim by §5.4. One asymmetry the implementer MUST reproduce: the 5m seed uses `get_data_path()` (existence-aware primary/fallback resolution) while the other 6 compose from `get_data_root()` — see C1. |
| V2 | **D1: all production fetches brain-continuous; sim/interface/harness untouched** | **CONFIRMED.** The 5 production fetch call sites — `data_manager.py:518` (backfill), `:695` (roll ratio), `:1001` (ledger), `live_trader.py:2393`/`:2456` (reconnect 5m/1h inside `_backfill_reconnect_gap_async`) — ALL pass `continuous=True` via `self.data_client` (the adapter). Binding the symbol at adapter construction is semantically exact. The parity harness constructs **`SimulatedDataFeed` only** (`livetest_engine.py:626`) and never `IBKRDataFeedClient`; it passes explicit 5m seed (`cl-5m_bk.csv`) + sim cache (`:708-717`), and the real 1h DataManager + seed-existence check in `LiveTrader.__init__` uses the CL defaults — byte-identical under D4, so the harness needs zero edits. Reconnect-backfilled bars flow into `data_manager_5m.append_bar/save_cache` (`:2414-2417`) — consistent with brain-symbol-keyed files. |
| V3 | **MCL coupling: no path puts MCL bars in CL files** | **CONFIRMED for bar data; ONE NEW FLAG.** Hands stream handler `_on_front_month_bar_update` (`live_trader.py:2549-2578`) writes ONLY to the per-cid telemetry SQLite (`log_raw_bar`) + in-memory `_front_month_last_close` — front-month bars can never reach the parquet cache/ledger. Post-T2, an MCL config's brain streams and reconnect backfills carry genuine CL bars into CL files (correct sharing). **FLAG (not in audit):** `front_month_id` is the EXECUTION front-month localSymbol (`live_trader.py:662-668` startup, `:2207-2209` rollover → e.g. `"MCLQ6"`), persisted as `last_front_month` in the roll-metadata file (`_save_roll_metadata` `:598`). A CL instance and an MCL instance sharing `.roll_metadata.json` would ping-pong `"CLxx"`/`"MCLxx"` → spurious `_detect_rollover` (`:667`) per restart → `_compute_roll_ratio` vs CL-continuous → ratio ≈ 1.0, inside `_ROLL_PRICE_TOLERANCE` (0.01), so **no ledger adjustment and no roll-history append** — i.e., operational churn (cache validation/backup, ROLLOVER warnings, Telegram noise), NOT misdata. Today's baseline is strictly worse (one global file shared by ALL symbols — blueprint B3's phantom-roll hazard). Zero MCL configs shipped. Condition C2: document + route normalization to T5. |
| V4 | **cid-merge CL gate correct; no other callers** | **CONFIRMED.** `_merge_legacy_cid_caches` defined `cli.py:31`, sole call `:256` (inside the `client_id != 1` branch). Glob `warm_start_cache_cid*.parquet` (`:43`); legacy cid caches are CL 5m bars by definition — every config ever shipped is CL (`"execution_symbol": "CL"` in all 20 `configs/strategies/*.json`, grep-verified), and `worktree_startup.py:68-69` lists `warm_start_cache_cid10/13.parquet` as live artifacts — the fleet hazard is real, not theoretical. Gate on `ctx.brain_symbol == "CL"`: for CL instances behavior is byte-identical (same resolved path, merge still runs); for MCL instances (brain=CL, shared CL cache) the merge remains CORRECT and still runs — gate on brain_symbol, not execution_symbol, is the right key (C8). New per-symbol cache names (`warm_start_cache_ES.parquet`) don't match the `_cid` glob — no reverse contamination. |
| V5 | **Constructor-churn census** | **CONFIRMED with corrections (same conclusion, bigger numbers in one file).** Production `DataManager(`: exactly 2 (`live_trader.py:331`, `:376`) — no script constructs it. Tests: `test_data_manager.py` has **15** constructions (audit said ×13; actual lines 114, 126, 140, 152, 162, 182, 191, 210, 226, 250, 281, 304, 319, 330, 339); `test_rollover.py` 1 fixture (`:45`); `test_data_manager_ratio.py` **0** — it uses `DataManager.__new__(DataManager)` (`:26/:179/:220`), bypassing `__init__`, so the required kwarg costs it nothing (audit's "constructs then overrides attrs" is imprecise but harmless). Total = 16 — matches the manager-approved "~16". `_ROLL_METADATA_PATH` patch sites in `test_rollover.py`: **8** (`:63, :76, :87, :102, :115, :128, :287, :314`), not 3 — all one file, all mechanically replaced by the new `roll_metadata_path` param (C4). `IBKRDataFeedClient(`: only `factories.py:9` repo-wide; `test_ibkr_adapters.py` constructs `IBKRExecutionClient` only. `LiveTrader` seed/cache kwargs: keyword-only; the only external passers are `cli.py:297-298` and `livetest_engine.py:716-717` (explicit paths — unaffected by None-defaults). |
| V6 | **Wrapper parity incl. `includeExpired=True`** | **CONFIRMED feasible; one precision (C3).** Legacy `build_cl_contract` (`ibkr_client.py:25-52`): continuous → `ContFuture(symbol="CL", exchange=exchange, currency=currency, includeExpired=True)`; month → `Future(symbol, lastTradeDateOrContractMonth, exchange, currency)` **without** `includeExpired`; missing-month ValueError text pinned verbatim in D6. `build_mcl_contract` identical shape. D6's registry default resolves CL/MCL exchange to NYMEX (`instrument_master.py:43/:61`) — byte-equivalent; `get_instrument` raises `ValueError(f"Unknown instrument symbol: {symbol}")` (`:327`) exactly as D6 promises. Wrappers keep `scripts/download_ibkr_history.py:95` (`build_cl_contract(continuous=True)`) working unchanged. D6 text specifies `includeExpired=True` only on the ContFuture branch — correct, and the Future branch must NOT gain it (C3 pins both branches). |

Additional verifications: dead imports confirmed dead — `data_manager.py:855-858` (**both** `build_cl_contract` AND `ib_bars_to_dataframe` are unused in that module; the audit claimed only the former — the whole import statement can go), `live_trader.py:2373` (both names unused), `ibkr_execution.py:3` (`build_cl_contract` unused). `ibkr_execution.py:72-73` NYMEX hardcode confirmed; registry fix is behavior-identical for CL/MCL and `tests/test_ibkr_adapters.py:86` (CL assertion) stays green. Front-month NYMEX hardcodes at `ibkr_client.py:625/:675` and `_EXPIRY_BUFFER_DAYS=6` at `:601` confirmed (buffer correctly left to T5). MCL constraint-(3) contradiction confirmed real: today's brain subscriptions pass `self._execution_symbol` (`:2045/:2056/:2317/:2328`) and the adapter maps MCL→MCL-continuous (`ibkr_data_feed.py:93-96/:135-138`) — the manager's ACK covers this deliberate change. Sequencing verified: `resolve_instrument_context` at `live_trader.py:279` precedes DataManager construction (`:331`); cli ctx resolution (`:225`) precedes the factories (`:287-290`). Intra-symbol 5m+1h roll-metadata sharing preserved by passing the same derived path to both managers. External CL-literal consumers (`check_vol.py`, `worktree_startup.py`, `scripts/diagnose_data_health.py`, `scripts/*shadow_log*.py`, `.gitignore`) are CL-only tools — D4 byte-identity keeps every one working. Not independently verified (non-blocking, T6-deferred): `smoke_test_pipeline.py:332-334` WARN/skip branch behavior for unrecognized cache names.

---

## 3. Conditions of approval (binding on the implementer once the human authorizes)

- **C1 — path-derivation asymmetry.** `derive_data_paths` must build the 5m seed via `get_data_path("raw/{sym}-5m_bk.csv")` (existence-aware resolution) and the other six via `get_data_root()` composition — exactly the current expressions. Test 16 must construct its expected values from those same expressions (as the audit proposes), not from hardcoded strings, so CL_DATA_ROOT-vs-fallback environments both pass.
- **C2 — roll-metadata CL/MCL cross-talk (new finding, V3).** Add a code comment at the `front_month_id` persistence site and a ticket/status flag: concurrent CL+MCL instances sharing `.roll_metadata.json` will ping-pong `last_front_month` (CLxx/MCLxx) causing spurious rollover detection churn (no misdata — ratio ≈ 1.0 within tolerance). Normalization of the stored identifier is T5 scope (rollover semantics); T2 must only document it. Zero MCL configs shipped, so nothing is live-affected.
- **C3 — wrapper parity tests pin both branches.** Tests 1–2 must explicitly assert `includeExpired=True` on the ContFuture branch AND its absence (ib_insync default) on the front-month Future branch, for both legacy wrappers vs `build_future_contract`.
- **C4 — census corrections.** 15 (not 13) `DataManager(` sites in `test_data_manager.py`; 8 (not 3) `_ROLL_METADATA_PATH` patch sites in `test_rollover.py` (`:63/:76/:87/:102/:115/:128/:287/:314`) — all to be migrated to the `roll_metadata_path` param. `test_data_manager_ratio.py` needs no `symbol=` churn (uses `__new__`), but verify none of its exercised methods newly read `self.roll_metadata_path`/`self.symbol` (currently they exercise ratio paths, not metadata I/O — expected zero-touch).
- **C5 — dead-import removal scope.** At `data_manager.py:855-858` and `live_trader.py:2373` BOTH imported names are dead — remove the full import statements (verified by grep; re-verify at implementation in case of drift).
- **C6 — interface honesty.** The diff description must state that `data_feed_interface.py:83` changes (default removal); the "byte-unchanged" guarantee applies to the fetch-method block and `SimulatedDataFeed`. `SimulatedDataFeed` must NOT be edited.
- **C7 — parity gate.** The post-green HS14B ledger parity re-run (audit §6) is BLOCKING, not advisory — `LiveTrader.__init__` and the entire data-path spine change; any non-PASS is a T2 regression. Same convention as T1 C3.
- **C8 — merge-gate key + test.** The cid-merge gate must key on `ctx.brain_symbol == "CL"` (not execution_symbol) exactly as §5.6 proposes, so MCL instances still merge legacy CL cid caches into the shared CL cache. Extend test 30 with the MCL-config-calls-merge assertion alongside CL-yes/ES-no.

## 4. Disposition of audit §7 items
1. MCL brain semantics — manager-ACKed; independently confirmed the ticket text "preserved" is factually wrong about current code, so the ACK is the load-bearing authorization. D3+5.5 coupling is coherent; the coupled-decision warning ("do not mix") is correct and honored.
2a. `ibkr_execution.py:73` 1-liner — manager-approved; verified behavior-identical for CL/MCL and converts non-CL from silent-no-trade to correct. Endorsed. 2b. `close_cl_position*` NYMEX injection (`ibkr_client.py:460/:501`) correctly OUT (T3 routing endorsed — it is the dangerous stuck-position exit path and deserves its own ticket's attention).
3. Test churn — approved by manager; census corrected in C4 (net total unchanged at ~16 constructions + 8 patch migrations).
4. Deferrals (backup filenames, smoke-test regex, config path overrides) — endorsed as stated; my preference on §5.3 backups: take the auditor's optional cache-stem change (it is CL-5m byte-preserving) or defer entirely — either is acceptable, implementer's choice, but do not do a third variant.

## 5. Summary
The proposal is the right shape: one naming authority, one builder, symbol bound once per process at the adapter, everything below it required-keyword and fail-loud, CL pinned byte-identical at three independent layers (unit path table, LiveTrader integration, CLI) plus the parity gate. Independent verification found zero design errors; findings were two census undercounts (C4), one documentation-precision issue (C6), one genuinely new latent hazard (C2 — operational noise only, strictly better than today's global sharing), and one test-pinning precision (C3). Blast radius is fully enumerated and fenced. **Blocked from autonomous approval solely by the Mandatory Human Authorization Guardrail (multi-component refactor). Recommend the human authorize; on ACK this is an APPROVE with C1–C8.**
