# T7 Impact Review — ES ops runway (COMBINED: audit.md + audit_hourly_only.md)
**Ticket:** `t7-es-ops-runway_07052026_0214`
**Reviewer:** Ticket-Impact-Reviewer | 2026-07-05 PT | HEAD `03218af` (verified: `git rev-parse` = 03218afd7cd1; src/, tests/, scripts/, configs/strategies/ working trees clean vs HEAD)
**Scope reviewed:** audit.md §1 (equity session calendar), §2.1 (1h seed copy), §6 (canary runbook) + audit_hourly_only.md (enable_5m_stream mechanism, trailing/watchdog/heartbeat changes, ES01B 1-field patch). audit.md §2.2 (5m provisioning) EXCLUDED — superseded by user ruling (no 5m acquisition). Manager rulings taken as given: mechanism + default-true ACKed; calendar-first sequencing ACKed; CL-5m/ES-1h trailing asymmetry user-ruled; tester owns the watchdog test seam.

# VERDICT: **APPROVE** (with 7 binding conditions, §3)

Constraint rulings (workflow §Rules of Engagement):
- **Interface Rule — NOT triggered.** No function signature changes anywhere. New dispatch arms in `session_calendar.py`, a new registry tuple constant, additive gates/guards inside `live_trader.py`, one new module constant, one optional config key read via `.get()` with hard `ValueError` validation.
- **Base Class Rule — triggered on `instrument_master.py`, JUSTIFIED.** The change is data-value-only on exactly 4 entries (ES/MES/NQ/MNQ at :94/:113/:232/:251). The consumer set of `session_hours_ct` is exhaustively enumerable (grep, whole repo): the `session_calendar.py` tuple-equality dispatch (:175/:195) and two test files. The T5 C4 doc block (`session_calendar.py:29-37`) explicitly sanctions this exact evolution ("the dispatch below already supports adding one … REQUIRED precondition for the ES launch (T7 checklist)"). Business justification (daily forced disconnect + Telegram spam ≈15:25-15:35 CT per T5 V4) is sourced and verified.
- **Refactor Veto — NOT triggered.** No component is rewritten. GLOBEX and grains calendar bodies untouched (dispatch is tuple-equality; CL/ZC tuples unchanged → byte-identity by construction). live_trader changes are additive gates around a default-true flag; CL path is the flag's default. Multi-file but not a multi-component rewrite.

---

## 1. Independent verification results (all 8 assigned items)

### 1.1 Equity tuple + calendar semantics — VERIFIED FAITHFUL
- T5 provenance confirmed verbatim at `t5-hours-watchdog-rollover_07042026_2305/impact_review.md` V4 (:38-41): 17:00 CT open, real daily 15:15-15:30 CT halt (CME/RJO/NinjaTrader sourced), 16:00-17:00 CT maintenance ≡ the GLOBEX Mon-Thu hour==17 ET branch, Fri 16:00 CT weekend close, blueprint's "16:15-16:30" = ET/CT transcription slip.
- `_EQUITY_SESSION = (("17:00","15:15"),("15:30","16:00"))` is a faithful encoding: segment 1 wraps midnight (17:00→15:15 next day), segment 2 is the 15:30-16:00 post-halt reopen; gaps = halt + maintenance. Critically, at HEAD the tuple is a **dispatch KEY, not a parsed engine** (module docstring: "no generic segment engine") — it must only be distinct from GLOBEX/GRAINS and descriptive. It is both. Registry field shape-checks (`test_instrument_master_live_fields.py:106-116`, HH:MM pairs) pass for it.
- Sweep-check of the proposed `_equity_market_status` body (America/Chicago, grains precedent — correct, the halt is CT-defined):
  | Probe | Path taken | Result |
  |---|---|---|
  | Tue 15:20 CT | `weekday<=4 and hour==15 and 15<=minute<30` | CLOSED halt ✓ |
  | Tue 15:35 CT | falls through all branches | OPEN ✓ |
  | Tue 16:30 CT | `0<=weekday<=3 and hour==16` | CLOSED maintenance ✓ |
  | Sun 16:00 CT | `weekday==6 and hour<17` | CLOSED weekend ✓ |
  | Fri 15:35 CT | falls through (Fri≥16 false; Mon-Thu-16 false; halt-minute false) | OPEN ✓ |
  | Edges: Fri 15:20 → CLOSED halt; Fri 16:30 → CLOSED weekend; Sun 17:05 → OPEN; Sat any → CLOSED; Mon/Thu 16:xx → maintenance | | all ✓ |
- Anchor claim verified: session opens are exactly Mon-Fri 15:30 CT + Sun-Thu 17:00 CT (Fri 17:00 correctly not an open). The grains walk-back shape (`_grains_session_open_anchor`, :137-159) transfers directly.
- Watchdog gate `!= "OPEN"` confirmed at `live_trader.py:3940`; only "OPEN" is byte-load-bearing; CLOSED strings are shape-pinned in T5 tests — new strings don't collide with the byte-frozen CL set.

### 1.2 The 3 T5 pin evolutions — EXACTLY RIGHT, NOTHING MISSED
Full grep of `tests/test_session_watchdog_rollover.py` for ES/NQ/MES/MNQ + full-repo grep for `session_hours_ct`/`_GLOBEX_SESSION`/`market_status`/`session_open_anchor` in tests:
- The ONLY tests pinning ES/NQ to GLOBEX behavior are the three named: `test_globex_family_dispatches_to_same_calendar` (:257-266, loop contains "ES","NQ"), `test_es_maintenance_break_modeled_closed` (:268-274, asserts `_HALT_STR` byte string; its own docstring marks the halt "deliberately NOT modeled" — written to be evolved), `test_session_open_anchor_none_for_globex` (:276-291, `_ES` in the instrument loop).
- Everything else survives untouched: the minute-by-minute CL DST sweep (:229-255, CL-only), `test_q1_cl_reopen_surface...` (:293-303, CL-only), grains suite, `test_unknown_session_shape_raises` (:480-486, fake tuple ≠ EQUITY → still raises), ES rollover tests (:649-668, roll fields untouched), ES bars_per_day pins (:1244-1248, 276/23 untouched), `test_instrument_master_live_fields.py:205` (micro==parent → forces MES/MNQ to move with ES/NQ, exactly as planned; no GLOBEX pin on ES exists there).
- C4 sanction: `session_calendar.py:29-37` documents the addition as the required T7 evolution. Confirmed.

### 1.3 Trailing blocker + fix — VERIFIED; FIX SOUND
- `:1100` `last_bar = self.rolling_df_5m.iloc[-1]` is UNGUARDED; `rolling_df_5m` is `Optional[...] = None` at `:445` and only populated by the 5m warm start (`:1955`) → in hourly-only mode it stays None → `AttributeError` on `None.iloc`.
- Reachable from the 1h callback: `_on_bar_update_1h` calls `_check_trailing_stop()` under `_ledger_lock` at `:2884-2885` (the 957ced7 path — commit present in HEAD's recent history). Early returns (`_trailing_activated`, `_entry_price is None`, `_atr_at_entry` checks, `:1092-1097`) mean the crash fires exactly when it matters most: in an open position.
- Proposed guard (primary-frame selection) is correct: extremes tracking is monotonic max/min accumulation (`:1103-1104`), side-correct (long→highest_high `:1114-1119`, short→lowest_low `:1120-1125`). Sourcing High/Low from the 1h frame preserves both properties and equals the 1h BacktestEngine's trailing resolution (parity-improvement claim verified against `ledger_parity_check.py:64-68`).
- `_atr_at_entry` has NO 5m dependency: set at entry from `signal.atr_at_entry`/bracket ATR computed on the inference `rolling_df` (`:3443`, `:3025-3047` — 1h frame for a 1h config) or restored from the ledger (`:1456`). Entry-time extremes seed from the inference frame (`:3446-3447`). The only other 5m reads in the trailing orbit are `_recover_inherited_position` (`:1468/:1493/:1501/:1507-1509` — all `is not None`-guarded; extremes seeding silently skipped when None → amendment item 5's primary-frame fix is right) and `_check_naked_position` `:4073-4074` (guarded, market-order fallback — cosmetic, as stated).
- `rolling_df_5m is None ⟺ hourly-only` holds at trailing time: recovery (Step 7b `:778`) and callbacks (post-`_subscribe` `:784`) both run after `_warm_start` (`:770`), which populates the 5m frame whenever the flag is true. In the parity harness the 5m mirror stays populated (`livetest_engine.py:220-227`) → the `if not None` form keeps the harness byte-identical. Verified.

### 1.4 Front-month order-pricing criticality — VERIFIED, HANDS STREAM SEED-FREE
- Chain confirmed: `_front_month_last_close` preferred for `current_price` at `:3013-3016`; flows into `strategy.evaluate(current_price=...)` (`:3194`), marketable-limit entry `limit_price=current_price` (`:3425`), TP/SL offsets (`:3411-3412`), `_entry_price` (`:3441`), and `_check_time_barrier(current_price=...)` (`:3055-3059`) → time-barrier exit `close_position(..., current_price=current_price)` at `:1326-1330` + telemetry `:1342`. ES01B's `live_config.exit_mode: "marketable_limit"` (config verified) makes barrier exits PRICED off it. Rollover staleness reset at `:2391` confirmed.
- Seed-independence confirmed end-to-end: `_subscribe_front_month` (`:2271-2278`) → `adapters/ibkr_data_feed.py:126-149` `continuous=False` branch → `get_front_month_contract` → `Future(localSymbol)` → qualify → subscribe. No DataManager, no seed, no warm-start artifact anywhere on the path. Callback (`:2753-2782`) touches only telemetry `log_raw_bar` + the close cache. STAYS in hourly-only mode — correct.

### 1.5 Watchdog re-point — VERIFIED, INCLUDING THE HAZARD MATH (+ one addition)
- `_check_stale_bars` (`:3921-3986`) anchors SOLELY on `_last_bar_time_5m` (`:3944`); `None → return False` (`:3945-3946`) → **hourly-only instances are permanently UNWATCHED without the re-point** — the audit's claim is exact. Also confirmed: nothing at HEAD watches `_last_bar_time_1h` (the "HEAD irony" — a 1h config's inference stream is unwatched today even with 5m flowing).
- 135-min derivation verified: 1h bars are open-time stamped (`:2846-2847`, bars[-2] completed bar), delivered at T+60 → normal staleness oscillates 60→120 min → 135 = 120 + the legacy 15-min margin. The 15-min constant would false-positive every hour.
- 195 hazard verified: ES at HEAD is GLOBEX (`instrument_master.py:94`; no EQUITY arm at `session_calendar.py:176-198`), anchor=None. Across Mon-Thu maintenance (16:00-17:00 CT), last bar = 15:00 CT stamp; GLOBEX reports OPEN from 17:00 CT; staleness runs 120→180 min until the 17:00 bar lands at 18:00 CT → 135 fires daily ≈17:15-18:00 CT. 195 = 180+15 clears it. The 15:15-15:30 halt is indeed a non-issue for the 1h watchdog (≤ ~95 min < 135).
- **Reviewer addition (C3):** the amendment understates the pre-calendar hazard — with GLOBEX + anchor=None, the **Sunday 17:00 CT reopen** presents ~50h staleness vs Friday's last bar while reporting OPEN → even 195 min false-positives once per WEEK (same shape as the Q1-pinned CL daily reopen). Calendar-first sequencing (already ACKed) eliminates it via the Sun-17:00 anchor; this strengthens the ruling — see condition C3.
- With the equity calendar landed, 135 is safe at every seam I swept: normal hours (60-120), maintenance reopen (anchor 17:00 caps at ≤60), halt (CLOSED-gated), post-halt (anchor 15:30), Sunday (anchor Sun 17:00).
- `test_stale_threshold_pin_15` (`:1274-1277`) pins only the 15 constant — survives untouched (see condition C7 for its docstring intent).

### 1.6 ES01B 1-field patch — TOLERATED BY EVERY FROZEN TEST
- Shipped config verified: `live_config` = exactly `{client_id: 1010, entry_mode: marketable_limit, exit_mode: marketable_limit}`, `bar_size: "1h"`, `execution_symbol: "ES"`.
- `TestES01BPatchedConfig` (test_config_generator_symbols.py:602-655): all sentinel pins are **enumerated-field assertions** — the only live_config touch is `cfg["live_config"]["client_id"] == 1010` (:650). No whole-object equality, no file SHA/hash anywhere in the file (grep: zero `sha`/`hashlib` hits).
- Flipped T1 pins (test_instrument_context.py:285-303 `test_es01b_shipped_config_resolves_as_es`, :330-342 `test_all_shipped_configs_resolve`): resolution-level + artifact-existence assertions only — resolver ignores live_config entirely.
- No src-side schema rejects unknown live_config keys: every consumer reads `.get()` (fleet_runner.py:188 requires client_id only; cli.py:206-210; configurable_strategy.py:190; live_trader.py:393). Adding `enable_5m_stream: false` passes everything. T6 surgical-patch precedent applies.

### 1.7 Seed copy — INDEPENDENTLY RE-VERIFIED (scratchpad script, conda trader, 2026-07-05)
- `ES_raw.parquet`: columns exactly `[DateTime, Open, High, Low, Close, Volume]`, dtypes `datetime64[ns]/float64×4/int64` — **byte-identical schema to CL_raw_1h.parquet** (programmatic compare). tz-naive. Cadence median=mode=1h, max gap 4d1h (weekend/holiday). 95,734 rows, 0 dups, 0 NaN. Range 2010-06-07 → 2026-06-30 23:00. Last Close 7544.75 (ES magnitude ✓).
- 292-day window: **4,696 bars anchored at file max** (audit's number reproduced exactly); **4,638 anchored at now (2026-07-05)** — the operative live-trim framing — still ≥ 4,320 (REQUIRED_1H_BARS verified at data_manager.py:114; ES lookback = derive_seed_lookback_days(23) = 292 verified by import). See condition C5 on decay.
- `derive_data_paths("ES")` (executed): `seed_1h = C:\CL_Analyst_Data\data\processed\ES_raw_1h.parquet` (exactly the copy target, currently ABSENT — no clobber), `seed_5m = C:\CL_Analyst_Data\data\raw\es-5m_bk.csv` (ABSENT — confirms the amendment's premise), `warm_start_cache_ES{,_1h}.parquet` ABSENT. No-Silent-Bootstrap hard fail confirmed verbatim at data_manager.py:293-308 (loud FileNotFoundError — the manager-ACKed failure mode). The 1h-side FileNotFoundError guard (live_trader.py:405-418) is equally loud. Nuance for the record: `live_config.seed_path_1h` override DOES exist at HEAD (live_trader.py:392-400) — the audit's "never implemented" claim was about `seed_path_5m` (correct, 0 hits) and §2.1 explicitly declines the 1h override in favor of the canonical name. Consistent.

### 1.8 Canary runbook greps vs real log formats — ALL REAL, ONE DEFECT
Verified at HEAD: `Instrument resolved: execution=%s (%s, tick=%s) brain=%s` (cli.py:230-234, pre-connect, pre-factory); `DATA PATHS: 5m seed=… cache=…` / `DATA PATHS: 1h seed=… cache=…` (live_trader.py:365/:403); `Loaded macro daily closes: %s` (:687); `Front-month contract: %s (month=%s)` (:697-701); `Cache seeded: %d bars` (data_manager.py:287); `1h rolling window initialized: %d bars` (:2015); `CACHE VALIDATION FAILED` (:2036); `Subscribed to 5-min/1-hour continuous contract live bars` (:2251/:2262); `Subscribed to front-month live bars` (:2278); `HEARTBEAT: alive | last_bar=%s | market=%s` (:3842); `NEW 5M BAR:`/`NEW 1H BAR:` at INFO (:2817/:2872); `STALE BAR WATCHDOG` (:3966); Telegram `LiveTrader Online … Dry-run:` (:796-806). Ports/cids verified: `--data-port` default **4001 (live)** with 7496 fallback (cli.py:132-135) — the runbook's explicit override warning is warranted; `--exec-port` default 4002; exec cid = data cid + 1 (cli.py:311) → 1010/1011; telemetry `live_telemetry_cid1010.db` (cli.py:260-266); log `reports/livetrader_1010.log` (log_config.py:76). Zero-CL grep patterns checked against benign strings: `CLOSED`, `CL_DATA_ROOT`, "CL Analyst" all dodge `symbol='CL'|Front-month contract: CL|\bCL[FGHJKMNQUVXZ][0-9]\b`; ES local symbols (ESU6 …) cannot match.
**DEFECT (condition C1):** the AMENDED criterion 8 relies on `RAW BAR` lines — but that is `log.debug` (:2773-2778) and the file handler is hard-set to `INFO` (log_config.py:78). Front-month bars are **invisible in the log file at HEAD**.

### 1.9 Consumer-map spot audit (amendment §1/§5/§7) — ACCURATE
Every cited guard/gate site re-read at HEAD and confirmed: `:726-728` Step 6 unguarded (1h twin IS guarded — asymmetry proves the need); `:889` `_shutdown` unguarded (see C2 — worse than stated); `:2407` rollover unguarded AND outside any try (AttributeError would propagate into the rollover path); `:2498-2512` cancels guarded; `:2517-2525` deferred 5m resubscribe unconditional (gate needed); `:2583`/`:2618-2621` reconnect 5m block naturally skipped/guarded; `:2809-2837` 5m callback structure exactly as mapped (log_bar `market_bars` gap confirmed; inference gated `== "5m"` at :2835); `:3802-3807` heartbeat reads only `_last_bar_time_5m` ("no bars received yet" forever — item 7 justified); `:2048-2051` warmup 1h-sourced; `_virtual_ledger` writes only via `_on_new_bar` stream param. Harness confirmed hourly-only already (`livetest_engine.py:213-236` wires ONLY `_live_bars_1h`; :223-227 mirrors into rolling_df_5m citing :1100). `ledger_parity_check.py:61-68` docstring confirmed STALE post-957ced7 (comment-only); `:145` `--disable-trailing` confirmed (trailing fenced by unit tests, not the gate).

---

## 2. Blast radius summary
| Surface | Change | Consumers checked | Risk |
|---|---|---|---|
| `instrument_master.py` (4 tuple values + 1 constant) | data-only | session_calendar dispatch + 2 test files (whole-repo grep — nothing else reads session_hours_ct) | LOW (3 sanctioned pin flips) |
| `session_calendar.py` (2 dispatch arms + 2 functions + message) | additive | live_trader `_get_market_status`/`_check_stale_bars`/heartbeat | LOW (CL/grains byte-identity by tuple construction) |
| `live_trader.py` (~7 gates/guards, 1 frame-selection, 1 constant, heartbeat branch) | additive, default-true | 14 test files stubbing the 5m path (census spot-verified) — assertions untouched when flag true | MEDIUM (behavior-bearing in the new mode; CL pinned by parity gate + existing suites) |
| `configs/strategies/ES01B_*.json` (+1 live_config key) | additive | TestES01BPatchedConfig, T1 flipped pins, resolver, cli, fleet_runner — all enumerated-field/`.get()` reads | LOW |
| Ops: `Copy-Item ES_raw.parquet → ES_raw_1h.parquet` | new file, target absent | data_manager parquet seed branch | LOW (validated §1.7) |
| Canary runbook | ops doc | gateway 4002, cids 1010/1011 vs running HS14B 1400/1401 | LOW; user-gated by design |

Non-negotiable invariants re-affirmed: CL byte-identity (flag default + tuple-equality dispatch + parity-gate re-run convention, both audits commit to it); no silent forks (flag loud-logged + Telegram-stamped, misuse → ValueError, missing-seed default → loud FileNotFoundError); scope guards (no harness/generator/fleet_runner/adapters edits).

## 3. Binding conditions on approval
1. **C1 (runbook fix, blocking for the canary doc):** replace amended criterion 8's `RAW BAR` evidence — unobservable at INFO file-log level (log_config.py:78 vs :2773 debug). Use instead: `NEW 1H BAR:` within ~65 min spanning an hour boundary, heartbeat `last_bar=` populated (valid only after amendment item 7 lands), and/or row growth in the telemetry `raw_front_month_bars` table. Do NOT bump the logger to DEBUG for this.
2. **C2 (:889 guard is functional, not cosmetic):** unguarded, the None-manager AttributeError is swallowed by the SHARED try/except (:888-893) and **skips the 1h `save_cache()` on the same block** — hourly-only shutdowns would never persist the 1h cache. Tester: assert the 1h save happens in the hourly-only shutdown test (amendment test 7 already implies it — make it explicit).
3. **C3 (sequencing is load-bearing beyond the stated window):** pre-calendar GLOBEX+anchor=None also false-positives the **weekly Sunday 17:00 CT reopen** (~50h staleness ≫ any threshold). The ACKed calendar-first order eliminates it; if the 195-min interim route is EVER taken instead, the runbook must list the weekly Sunday reconnect as expected, and 195 must drop to 135 when the calendar lands.
4. **C4 (trailing fix shape):** select the frame once (`rolling_df_5m if … is not None else rolling_df_1h` or the `_primary_bar_df()` helper) with no flag read at :1100 — keeps the parity harness (populated 5m mirror) and all CL trailing tests byte-identical, and `None ⟺ hourly-only` holds by construction (verified §1.3). Extremes must remain the max/min accumulators — no reseeding on the swap.
5. **C5 (seed copy timing):** now-anchored 292d coverage is 4,638 today (not the file-max 4,696) and decays ~23 bars/trading day; startup backfill covers the seed→now gap regardless. Do the copy and run the canary in the near term; re-run the audit's §2.1 validation script at copy time.
6. **C6 (scope guards, re-affirmed):** do not touch the livetest 5m mirror; do not fix the pre-existing `:2527 == "1h"` vs `:2253 in ("1h","2h","4h")` deferred-resubscribe inconsistency in passing (unpinned; route to a micro-ticket with the deferred items); `ledger_parity_check.py:64-68` stale docstring is comment-only (fix in passing acceptable).
7. **C7 (TDD-manager flag):** `test_stale_threshold_pin_15`'s assertion survives, but its docstring intent ("15 minutes for ALL instruments") is evolved by the mode-driven threshold — docstring-context note only, no assertion change; pair with the amendment's new `_STALE_BAR_THRESHOLD_MINUTES_1H` pins.

Items that remain human/role-gated by their own design (NOT approval blockers; already routed): gateway canary go-ahead + artifact creation + Telegram traffic (audit §6/§8.2); TDD-manager sanction of the 3 pin evolutions (normal T6-precedent flow, §1.2 confirms the set is exact and complete); CME entitlement purchase only if the §5 signatures fire.

## 4. Decision
**APPROVE** the combined T7 scope (equity calendar + 1h seed copy + hourly-only mode + ES01B 1-field patch + canary runbook as amended), subject to conditions C1-C7. Both audits' riskiest claims re-verified true at HEAD; the one defect found (C1, runbook observability) is documentation-level; two findings sharpen rather than contradict the proposals (C2 shutdown-save coupling, C3 Sunday-reopen hazard). Regression assessment concurs: NO regressions at HEAD; the :1100 read is correct today and becomes a defect only in the new mode.
