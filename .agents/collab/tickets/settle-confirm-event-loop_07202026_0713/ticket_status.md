# Ticket Dashboard — settle-confirm-event-loop_07202026_0713

**Ticket Directory:** `.agents/collab/tickets/settle-confirm-event-loop_07202026_0713/`
**Source:** live incidents 2026-07-19 16:00 PT (GC/MGC, event GC_Sharpe_E04_07102026_800d842eb78e)
and 2026-07-20 07:00 PT (CL, event HS14B_Sharpe_E01_06262026_e778ce9f6b31) — 2nd
confirmed occurrence. The a1464d2 TIME BARRIER exit confirm-gate (`_confirm_settled_position`)
calls `get_position_settled` -> `self.ib.run(...)`, which requires an idle asyncio
event loop, but the exit fires from inside the live bar-processing loop ->
`RuntimeError: This event loop is already running` -> gate returns None -> exit
fail-closes (retains protection, defers to housekeeping) every time. Deterministic:
both occurrences are the first TIME BARRIER exit each child took since the fix
deployed 07-16. NOT a naked-position regression (fail-safe both times) but the
confirm-gate never books a clean exit; housekeeping OOB-closes with a NULL price
each time (ledger corruption side-effect: trade_21 GC, trade_116 CL so far).

[2026-07-20T07:14:00-07:00] | settle-confirm-event-loop_07202026_0713 | TICKET-MANAGER | STATUS: Ticket minted, workspace created. Spawning Ticket-Auditor with both incident logs + the a1464d2 confirm-gate design context. Waiting for root-cause + proposed fix.
[2026-07-20T07:26:00-07:00] | settle-confirm-event-loop_07202026_0713 | TICKET-MANAGER | STATUS: Auditor replied. ROOT CAUSE CONFIRMED: _confirm_settled_position (:1806, :1875, sibling :1657) calls get_position_settled -> self.ib.run() which requires an IDLE asyncio loop, but fires from inside the ib_insync bar-update callback (running loop) -> RuntimeError -> fail-closed every time. ARCHITECTURAL finding: even without the RuntimeError, an in-loop cache read would return STALE pre-exit position (the just-submitted exit's fill event is queued behind the current callback) -> confirm must happen on a LATER IDLE TICK, not inline. Codebase already solved this class once (_deferred_resubscribe :4187, same RuntimeError noted in its own comment). PROPOSED FIX: defer the settled-decision block to a new idle-loop reconciler invoked between ib.sleep() calls in the main loop (beside housekeeping/rollover checks) — reuses a1464d2's decision logic byte-for-byte (fail-closed/None, proven-price book, Binding Condition 1), only the CALLING CONTEXT moves. Rejected alternatives grounded not assumed (inline cache read=stale/unsafe; in-method loop.is_running() fallback=not a genuine fix). Fallback direction offered (async-native call_soon) but higher blast radius, not first choice. SEVERITY HIGH, CONFIRMED REGRESSION (a1464d2, 07-16) -> per protocol NOT fast-tracked regardless of severity. CRITICAL TEST FINDING: existing suite MOCKS AWAY the exact primitive (ib.run()) that raises -> cannot catch this bug class; new loop-aware test infra required (Auditor flagged explicitly rather than claim false coverage). Spawning Ticket-Impact-Reviewer (severity/regression status WITHHELD for unbiased review). Waiting.
[2026-07-20T07:38:00-07:00] | settle-confirm-event-loop_07202026_0713 | TICKET-MANAGER | STATUS: Reviewer verdict = REQUEST HUMAN AUTHORIZATION (Refactor Veto triggered — relocates the exit-confirmation state machine across the async-callback/main-loop boundary + new cross-tick pending-exit state + new ordering contract vs housekeeping/kill-switch, on a live real-money path shared by all 5 children). Reviewer independently verified ALL Auditor claims from source (in-loop call chain, 2-safe/3-hazardous call sites, stale-read crux, _deferred_resubscribe precedent, 5s poll cadence, parity untouched, test-mock-bypass). Direction A (idle-loop reconciler) endorsed as correct/lower-blast-radius vs Direction B (async task), no forbidden shortcuts found. 4 BINDING CONDITIONS attached for authorization: (1) reconciler must run BEFORE housekeeping/kill-switch each poll — ordering must be pinned; (2) _check_time_barrier must be re-entrancy-aware while _pending_exit_order_id is set (no double-submit/re-crash during deferral); (3) GAP FOUND: the :1657 first-flat-read site triggers on a flat read with NO pending time-barrier exit -> NOT covered by a reconciler gated on _pending_exit_order_id — folding it in as-proposed would silently drop OOB-close confirmation in the common case, needs a separate trigger; (4) new loop-aware regression test mandatory (plain-int mock cannot prove the fix), no test loosening. Reviewer recommends: authorize Direction A with all 4 conditions + require a canary before redeploy. HALTING per protocol Step 4.2 — presenting to human, will NOT proceed to blueprint/TDD until explicit authorization.
[2026-07-20T09:30:00-07:00] | settle-confirm-event-loop_07202026_0713 | TICKET-MANAGER | STATUS: OPERATOR AUTHORIZED 2026-07-20. Human-authorization gate CLEARED. BLUEPRINT WRITTEN -> blueprint.md, Direction A (idle-loop reconciler) with all 4 Reviewer binding conditions baked in: (1) reconciler runs BEFORE housekeeping each poll; (2) _check_time_barrier re-entrancy guard while _pending_exit_order_id set; (3) :1657 flat-read site gets its own trigger in the reconciler (not pending-exit-gated); (4) new loop-aware regression test tests/test_settle_confirm_loop_deferral.py. Verified current line anchors: 3 hazardous sites (:1657, :1806, :1875 in-loop) + 2 safe startup sites (:2216, :2715); main-loop idle wire-in slot at :5576 (beside housekeeping/roll-resolution); _POLL_INTERVAL=5.0 :137. Handing to /tdd-manager for implementation (operator wants it fixed, will restart to deploy). Canary required before fleet redeploy.
