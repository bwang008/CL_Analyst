[2026-07-04 17:52:17] | livetest-macro-pctile-slow_07042026_1748 | TICKET-AUDITOR | RCA complete: per-bar livetest slowness root-caused to 12x rolling.apply(lambda rank) passes over the full 13,138-row FRED daily history (10.58s/bar measured in trader env, pandas 1.5.3) inside _build_fred_features (src/features/macro_features.py:486), invoked per replay bar via build_live_features->merge_all; git blame f9122b28 (2026-03-22) shows this is long-standing code, NOT a recent regression; proposed drop-in replacement with native Cython series.rolling(w).rank(pct=True) (available since pandas 1.4), empirically verified BITWISE-IDENTICAL (max_abs_delta=0.0, identical NaN masks) on real FRED data incl. live-override row, tie/NaN edge cases, and COT 52/14/35W windows, with 339x measured speedup (59min->10.5s for 336-bar replay pctile cost); severity MEDIUM (mechanical multi-line change, 4 blocks in one file + optional pandas>=1.4 floor bump in requirements-dev.txt); rejected caching (unneeded once vectorized) and multiprocessing (complexity in production LiveTrader path) alternatives.
[2026-07-04 17:57:58] | livetest-macro-pctile-slow_07042026_1748 | TICKET-IMPACT-REVIEWER | APPROVED: blast radius mapped (macro_features.py is the sole file containing the lambda pattern, 5/5 occurrences covered; consumers data_processor.py x11 sites, feature_pipeline.py:258, live_trader.py:676/3652, livetest harness all consume bitwise-identical outputs; ES/TFF normalization happens upstream in scripts/download_macro_data.py so _build_cot_features is report-type-agnostic); independently re-verified equivalence in trader env (pandas 1.5.3) on real FRED (13,138 rows, 24 col/window/path combos incl. live-override injection) + real COT (5 blocks) + 48 adversarial synthetics = ALL bitwise identical (max_abs_delta=0.0, identical NaN masks), 360x measured speedup; no Interface Rule breach (signatures/returns unchanged), Base Class concern satisfied by strong business justification + verified numerical identity, no Refactor Veto (single-component change + one-line dev requirements floor bump); conditions: implementer must add the equivalence-pinning unit test, bump requirements-dev.txt pandas>=1.4.0, and re-run 336-bar parity livetest ledger-identity check per livetest.md Step 5 before merge.
