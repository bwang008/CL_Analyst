# Ticket Audit Log — optimizer-objective-report-parity_07032026_0830

[2026-07-03 19:20] | optimizer-objective-report-parity_07032026_0830 | TICKET-MANAGER | Dispatched a background Ticket-Auditor sub-agent; it ran async and did not return usable inline output, so per coordinator direction the Auditor + Impact-Reviewer passes are performed in SELF mode (sequential, strict role separation).

[2026-07-03 19:25] | optimizer-objective-report-parity_07032026_0830 | TICKET-AUDITOR | RCA complete. Verified in reports/batch_runs/batch_20260703_0758 that ALL 8 individual + 4 ensemble targets have regression_guard_triggered=True for BOTH objectives; guard reverts best_result/best_metrics to the objective-independent baseline (strategy_optimizer.py ~L1359-1361), so identical Sharpe/Sortino trade/PnL/metrics is CORRECT, not a trial-selection bug. Genuine defect: generate_optimized_report() individual-mode path (batch_post_optimizer.py ~L557-577) falls back to all_trial_params (the DISCARDED trial) to fill the Opt Thr/TP/SL/... columns while Best Trial prints "baseline (guard)" — so those columns show rejected, per-objective-differing params instead of mirroring baseline/pre. Ensemble path (generate_ensemble_artifacts.py L403 `if not regression_triggered`) already gates params correctly; ensemble summary uses long_params/short_params ({} under guard) so it is unaffected. Proposed localized fix: suppress the all_trial_params fallback when regression_guard_triggered so guarded rows show pre==opt (baseline or '-'). Severity: MEDIUM (report-correctness logic, not one-liner). Not a fresh regression of the parity symptom — guard/params behavior is longstanding; format_best_trial "Fix 1" (tests/test_report_best_trial.py) established the intent that guarded Opt columns are "intentionally blank (pre==opt)", which this path violates.
[2026-07-03 19:16:22] | optimizer-objective-report-parity_07032026_0830 | TICKET-AUDITOR | Verified batch_20260703_0758 artifacts: 8/8 individuals + 2/4 ensembles hit objective-independent regression guard so identical Sharpe/Sortino metrics are CORRECT (no objective-wiring bug); true defect is a REPORTING bug -- individual-report all_trial_params fallback (batch_post_optimizer.py ~557-563 and Detail ~631-639) leaks DISCARDED per-objective trial params into Opt columns under a "baseline (guard)" label, violating format_best_trial contract; ensemble path correct (blanks via empty long/short_params). Proposed guard-gating the fallback. MEDIUM; recent regression seam (ced3642 2026-07-01).
[2026-07-03 19:32] | optimizer-objective-report-parity_07032026_0830 | TICKET-IMPACT-REVIEWER | Fresh unbiased blast-radius review (formed WITHOUT reference to the Auditor severity call). Interface Rule: NOT triggered (no signature change; fix is internal to generate_optimized_report). Base Class Rule: NOT triggered (no base class / shared utility touched). Refactor Veto: NOT triggered (single function, single file batch_post_optimizer.py; two localized blocks). Consumers of all_trial_params limited to batch_post_optimizer.py reader + strategy_optimizer.py writer; no external module depends on Opt-column population semantics. opt_info (carrying regression_guard_triggered) is in scope at both fallback sites. Existing coverage: tests/test_report_best_trial.py + tests/test_ticket_blueprint_v2.py; recommend one new assertion that a guarded individual row's Opt columns mirror baseline/'-' and are objective-invariant. DECISION: APPROVE.
