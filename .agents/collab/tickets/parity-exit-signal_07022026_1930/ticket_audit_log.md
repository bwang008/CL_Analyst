<!-- Migrated 2026-07-03 from shared .agents/collab/ticket_audit_log.md (per-ticket log convention). -->
[2026-07-02T19:52:00-0700] | parity-exit-signal_07022026_1930 | TICKET-AUDITOR | RCA of residual backtest-vs-livetest parity divergence (post-OCA). Phenom B (2026-06-02 exit flip): DROPPED FROM SCOPE — commit 922dea5 (today) moved _check_trailing_stop() from _on_new_bar (line 2952) to the ONLY call site _on_bar_update_5m:2617, which never fires in 1h Parity-Mode livetest (driver fires _on_bar_update_1h only), so trailing stop is now dead in the livetest; owned by the in-flight trailing-stop-5m ticket. Phenom A (18-vs-17 count + 1-bar-early short): MEDIUM, NOT a regression — root cause is the livetest's double-cooldown (ConfigurableStrategy.evaluate:418-441 pre-gate + TieredEnsembleStrategy:754-760) papered over by a fragile counter/on_exit monkey-patch in livetest_engine.py:730-776; proposed removing the ConfigurableStrategy pre-gate so a single cooldown authority matches the backtest. Phenom C: LOW, accept as tolerance (1-tick rounding of round(new_sl,2) + gap-fill), document, no code change.
[2026-07-02T20:05:00-0700] | parity-exit-signal_07022026_1930 | TICKET-IMPACT-REVIEWER | REQUEST-HUMAN-AUTHORIZATION on Phenom A. Interface Rule: PASS (evaluate() signature and EngineState ctor unchanged; both live evaluate() call sites 2014/2957 use kw args; backtest uses EngineState() directly and never constructs ConfigurableStrategy). Base Class Rule: contained — ConfigurableStrategy built only in livetest_engine.py:628 + PRODUCTION cli.py:197 + smoke_test:353; removing evaluate() pre-gate changes livetest AND live-production cooldown behavior, NOT backtest. Refactor Veto: TRIGGERED — step 2 requires NET-NEW state in LiveTrader (no last_exit_bars_ago/bars-since-exit counter exists; only _position_bars_held tracks in-position bars), so change spans configurable_strategy.py (delete pre-gate + its only counter maintenance) + live_trader.py (build per-side since-exit counter, wire into both evaluate() paths) + livetest_engine.py (delete monkey-patch). Multi-component refactor touching a live-production entry-gating path => guardrail forbids autonomous approval. B-drop VERIFIED correct (test_trailing_stop_5m_scheduling.py header Strict-Lock:TRUE locks _on_bar_update_5m/_on_new_bar). C doc-only, zero blast radius.
[2026-07-02T20:34:00-0700] | parity-exit-signal_07022026_1930 | TICKET-AUDITOR | REVISED per human decision. Phenom A: RECOMMEND OPTION B — keep ConfigurableStrategy.evaluate as SOLE cooldown authority, confined to configurable_strategy.py: (1) move counter increment (currently 434-441, post-gate) to start-of-bar BEFORE the 418-441 gate to match backtest_engine.py:1200-1201; (2) neutralize downstream TieredEnsembleStrategy 754-760 re-gate by feeding sentinel last_exit_bars_ago_*=9999 into the EngineState built at 443-449 (evaluate fully owns that object — mechanically feasible, NO live_trader state); (3) delete livetest_engine.py:762-776 monkey-patch. Rejected Option A (needs NET-NEW per-side since-exit counter in live_trader + both evaluate() sites incl cli.py:197; only _position_bars_held exists, opposite semantic). B stays confined to 1 prod file, no new state, no live order-gating path signature change. Phenom B: RE-INVESTIGATED, STAYS IN THIS TICKET — static-SL price divergence: live SL=round(fill_price±sl_offset,2) at live_trader.py:1622/1635 (fill_price=entry+slippage, sl_offset itself pre-rounded via strategy sl_price round at configurable_strategy.py:547/550 → DOUBLE-rounded) vs backtest SL=entry_price∓sl_mult*atr (raw close, UNROUNDED) at backtest_engine.py:661/666; live long-SL sits ~1 tick higher(closer) → fires where BT rides to barrier. Compounded by intrabar precedence inversion: live matches SL in sim on_bar_feed BEFORE _check_time_barrier(2824); backtest checks TIME_BARRIER(688) BEFORE SL(694). Backtest correct on rounding+slippage-basis. Phenom C: 06-04 SL & 06-09 SL trailing-dependent → UNMEASURABLE-HERE (live inert in 1h harness); 06-01 TP $14.04 static gap-fill/round sub-tick → accepted tolerance.
[2026-07-02T20:38:00-0700] | parity-exit-signal_07022026_1930 | TICKET-IMPACT-REVIEWER | REVISED decision. A (Option B): APPROVE. Independently verified last_exit_bars_ago_* is read ONLY at execution_models.py:755/758 (the two cooldown checks) and NOWHERE else in on_bar — sentinel-feed 9999 neutralizes exactly the double-cooldown with zero collateral on tier-match/conflict/consecutive logic (those read .position/.side/.open_positions/.bars_held, untouched). Change set = 1 prod file (configurable_strategy.py: relocate increment above gate + feed sentinel EngineState 443-449) + delete harness monkey-patch livetest_engine.py:762-776. No new LiveTrader state, no signature change, EngineState ctor unchanged → CLEARS Refactor Veto. Reachable in prod via cli.py:197 but effect is strictly removal of redundant double-gate + backtest-alignment; recommend a live-vs-backtest parity assertion on the SHORT→release→LONG boundary before merge. A APPROVED. B: REQUEST-HUMAN-AUTHORIZATION. Verified (a) BT SL unrounded from raw entry (backtest_engine.py:661/666) vs live round(fill∓sl_offset,2), sl_offset from already-rounded signal.sl_price (cfg:547/550 → live_trader:3176 → 1622/1635) — real double-round+slippage skew, and 1622/1635 IS the production bracket-placement path; (b) precedence INVERSION confirmed — BT TIME_BARRIER first (688) then SL/TP (694+); live runs matching-engine SL/TP in sim on_bar_feed (livetest_engine.py:353, simulated_execution.py:522-537) BEFORE the callback, time-barrier later in _on_new_bar (2824). Recommend: (a) change BACKTEST to round SL/TP to 2dp on slippage-adjusted entry to mirror live — localized but alters BT exit-price basis feeding reported PnL/metrics across ALL backtests (cross-consumer blast radius on reported numbers → needs sign-off); (b) NOT a localized ordering tweak — it changes same-bar exit ATTRIBUTION across two engines (structural same-bar-precedence decision affecting exit-reason labeling repo-wide) → Human-Authorization. B split from A; do not block A on B. C: doc-only, zero blast radius, corrected framing accepted.
[2026-07-02T22:18:33] | parity-exit-signal_07022026_1930 | TICKET-MANAGER | Blueprint finalized (A + B(a) authorized; B(b)/C backlogged) and handed to human for /tdd-manager.
