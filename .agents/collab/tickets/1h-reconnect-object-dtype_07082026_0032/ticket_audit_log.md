[2026-07-08T08:04:45Z] | 1h-reconnect-object-dtype_07082026_0032 | TICKET-AUDITOR | RCA CONFIRMED: fleet hourly-blindness = object-dtype OHLCV. Reconnect-backfill (live_trader.py:3624/3685) iterates ib_bars_to_dataframe chunk_df (DateTime col + float OHLCV) via new_bars.iterrows() -> each row is object-dtype Series (mixed Timestamp+float); row.to_frame().T yields all-object OHLCV; pd.concat in DataManager.append_bar (data_manager.py:484) upcasts cache Close to object; alpha_factory.py:180 np.log(Close/shift) crashes 'loop of ufunc does not support argument 0 of type float'. Reproduced byte-for-byte in trader env (pandas 1.5.3). NOT a recent regression: cece7d55 (2026-06-01) + 465af9c2 (2026-02-24). Proposed fix: coerce OHLCV to numeric (errors=raise, loud-fail) inside append_bar after the to_frame().T, at the ingestion seam -- alpha_factory untouched, backtest parity sacred. Severity HIGH (fleet-wide hourly signal skip, all 5 children).

[2026-07-08T08:22:10Z] | 1h-reconnect-object-dtype_07082026_0032 | TICKET-IMPACT-REVIEWER | APPROVE. Independent blast-radius map: fix touches ONE method (DataManager.append_bar) at the sole ingestion choke point; NO signature change (in-place mutation, void return), NO base-class/inheritance impact, NO multi-component refactor -> no Interface/BaseClass/Refactor veto tripped, no human-gate trigger. Reproduced the object-dtype upcast + np.log ufunc crash byte-for-byte in trader env (pandas 1.5.3), then verified proposed coercion (to_numeric errors=raise) restores float64 and unblocks np.log. Confirmed the crash-feeding df at live inference is get_ratio_adjusted_df()->copy of _df (live_trader.py:3964), i.e. exactly what append_bar poisons -> fixing append_bar fixes the real crash. Verified the parallel rolling_df_1h/5m reconnect concat (live_trader.py:3675/3613) is NOT poisoned (it concats properly-typed sliced new_bars, not iterrows output) and is only a non-1h fallback -> no second open object-dtype path into features. Edge cases all pass: existing float64 callers unchanged (steady-state 3831/3889 + dup-append tests), corrupt bar raises LOUD (project no-silent-null rule honored), NaN volume passes, Series seam (data_manager.py:473) also covered. Backtest/training never call DataManager.append_bar (livetest_engine.py has its own independent mock) -> alpha_factory + backtest parity byte-identical. Minor non-blocking note: string-numeric coerces Volume to int64 not float64, harmless (real reconnect path yields float64). Require the two proposed tests (red repro + loud-fail) land green.
