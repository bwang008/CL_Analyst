[2026-07-08T14:46:50Z] | reconnect-false-flat-oob_07082026_0731 | TICKET-AUDITOR | Investigated SI naked-short false-flat: RCA = ibkr_client.get_cl_position reads ib_insync's unwaited in-memory positions() cache; after a restart/reconnect the connectAsync reqPositions sync can time out (5s) leaving the cache EMPTY -> get_position returns 0 -> _recover_inherited_position(live_trader.py:1831) treats it as OOB close and cancels the still-live SL/TP. Long-standing (not a regression; core read e975bbef 2026-02-24, OOB branch 2e55132e 2026-03-26). Proposed fail-CLOSED fix: force reqPositions()+settle and re-verify a flat reading before trusting 0 at the cancel/close seams. Severity HIGH.
[2026-07-08T14:52:00Z] | reconnect-false-flat-oob_07082026_0731 | TICKET-IMPACT-REVIEWER | Verdict APPROVE WITH MANDATORY CONDITIONS. Independently confirmed RCA: ib.positions() reads wrapper cache with no request/wait (ib.py:429-441); blame matches (e975bbef/2e55132e, not a regression). Blast radius: adding get_position_settled as a NON-abstract default on ExecutionInterface (mirroring get_cached_position, base.py:62) does NOT trip Interface/Base-Class veto — no signature change to get_position, sim adapter unaffected. NOT a multi-component refactor (single new method + guarded call sites), so no human-auth required. PARITY SAFE (live-exec seam only, alpha_factory untouched). MANDATORY FIX CORRECTION: proposal's reqPositions()+ib.sleep(_SETTLE) is WRONG — blocking IB.reqPositions() waits on positionEnd but IB.RequestTimeout defaults to 0 => hangs FOREVER on a slow gateway (the exact incident), NOT fail-closed; and the broker_audit sleep-style variant can still read empty. Coder MUST use reqPositionsAsync() under an explicit asyncio.wait_for(_SETTLE) (or util.run(..., timeout=_SETTLE)) so positionEnd resolves deterministically OR TimeoutError => retain protection LOUD. Kill-switch (_check_naked_position ==0) already fails safe (returns, no cancel) — do NOT add a blocking settled read there. Startup recovery / _check_time_barrier / _cancel_orphaned_orders_on_startup guards approved.
