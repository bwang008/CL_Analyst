[2026-07-21 18:51] | protective-leg-fill-race_07212026_1851 | TICKET-AUDITOR | Second-opinion audit (independent of a withheld candidate fix) of the software-side TP/SL sibling-cancel design in src/live_execution/. Investigated from source + full git history (unbounded git log -S, not the restricted 5-commit scope — authorized by the task's explicit "reconstruct the historical design" mandate).

FINDING 1 (historical reconstruction): commit 0a983d5 (2026-03-18) introduced place_child_orders using native parentId bracket linkage (tp.transmit=False, sl.transmit=True, both parentId=parent_order_id) -- the classic IB bracket pattern. One week later, commit 4a50a4f (2026-03-25) ripped parentId out entirely: because this codebase places TP/SL AFTER the entry fills (two-phase, prices computed from actual fill price), by submission time the parent order is already Filled/terminal at IBKR, and referencing it via parentId causes a hard rejection (IBKR Error 201 "Parent order is being cancelled"), reliably triggered by fast/split fills. Docstring evidence: ibkr_client.py:1587-1596. The fix replaced ALL broker-side linkage with hand-rolled "software OCA" (live_trader.py _on_order_status, now _on_standard_execution_event) that cancels the sibling only AFTER observing a fill. Native IBKR OCA groups (ocaGroup/ocaType) were NEVER used at any point in this repo's history -- the historical problem (parentId + terminal parent) is orthogonal to ocaGroup, which needs no parent reference at all.

FINDING 1b: a second, unrelated historical change (064a776, 2026-05-13) removed strategy-driven NET_TO_ZERO/FLIP exits (a live signal could emit a competing EXIT order against an open position) in favor of "bracket-only exits" (TP/SL/Trailing/Time-Barrier are the ONLY exit paths). Any fix here must preserve that invariant -- confirmed my proposal does not touch strategy/signal code.

FINDING 2 (Race A -- sibling TP/SL, the core of this ticket): ibkr_client.py:1572-1641 place_child_orders submits TP (LMT) and SL (STP, triggerMethod=1) as fully standalone orders, no parentId, no ocaGroup -- broker-side, nothing links them. live_trader.py:6439-6472 (_on_standard_execution_event) observes a Filled event on either tracked id, THEN fires a fire-and-forget bulk cancel_open_orders (ibkr_execution.py:284-313 -> ibkr_client.py:687-706, plain ib.cancelOrder(), no ack wait) for the sibling, and immediately calls _reset_position_state() (live_trader.py:1271-1297), which clears _tp_order_ids=[] and _sl_order_id=None. Both legs rest with outsideRth=True (ibkr_client.py:1621,1634) i.e. live through the thinnest, gappiest hours. If the sibling ALSO fills at the broker before the cancel lands, its (later) fill event no longer matches any tracked id and falls into the "else" branch at live_trader.py:6473-6490 -- logged as "[TRADE] UNRECOGNIZED FILL" at ERROR level ONLY: no Telegram, no health event, no correction. Net broker effect: full quantity closed twice in the same direction = REVERSED position, completely unprotected (both legs consumed).

FINDING 3 (detection gap): _check_naked_position (live_trader.py:6234-6260) early-returns at :6248-6249 whenever self._active_trade_id is None -- which is ALREADY true post-reset in the Race-A sequence above, so the ~poll-interval kill switch structurally cannot see a reversed position. The only net is the HOURLY _housekeeping_sweep's UNTRACKED/NAKED branch (live_trader.py:3177-3196), explicitly detect-only ("stays human-only" per its own comment) -- up to ~1h of a naked, reversed, fully unmanaged live position, alert-only.

FINDING 4 (the codebase already half-knows this hazard class): _reconcile_pending_position_state's BINDING CONDITION 1 (live_trader.py:1857-1865) explicitly documents "ib_insync fires cancelOrder fire-and-forget ... and a fast fill can still cross at the exchange (the race documented at ibkr_client.py:1583-1588)" and applies a re-scan-before-re-arm guard -- but ONLY for deciding whether to re-arm protection after retiring a STRANDED, UNFILLED time-barrier exit order. This discipline was never extended to (a) the original sibling-cancel-on-fill path (Finding 2), or (b) _check_time_barrier's OWN cancel-then-immediately-submit-new-exit sequence (Finding 5).

FINDING 5 (Race B -- exit vs. stale resting leg, TIME BARRIER path): _check_time_barrier (live_trader.py:1717-1738) fires the same fire-and-forget cancel_open_orders, THEN immediately (same callback tick) submits a brand-new full-quantity closing order via close_position, with _sl_order_id/_tp_order_ids cleared at :1731-1732 BEFORE the new exit is even submitted. If a resting leg fills anyway (cancel lost the race), its fill event is likewise unrecognized (ids already cleared) while the new exit ALSO closes the position -- same double-close/reversal class via a different trigger (a submitted exit racing a not-yet-torn-down protective leg, matching the task's "exit-vs-resting-leg" scenario explicitly).

FINDING 6 (parity/test blind spot): adapters/simulated_execution.py:588-650 (on_bar_feed) enforces STRICT mutual exclusivity between TP/SL ("if BOTH trigger on the same bar -> SL wins", loser popped from the dict, zero-latency, zero-race) to match BacktestEngine's pessimistic convention. The entire existing SimulatedExecution-backed regression suite is therefore STRUCTURALLY INCAPABLE of reproducing this race -- it can only ever manifest against the live IBKR adapter. This explains why it has never been caught.

FINDING 7 (verifying the internally-cited prior incident): read the FULL, already-approved blueprint for reconnect-false-flat-oob_07082026_0731 (the $296k SI naked short). Its root cause is VERIFIABLY DIFFERENT from Race A/B: a stale/unpopulated ib.positions() cache on reconnect made the app believe a real, still-open position was flat, so it PROACTIVELY cancelled that position's genuine, still-good TP/SL -- leaving it NAKED (unprotected), not REVERSED (no double-fill occurred; nothing on the other side ever filled twice). That mechanism is already fixed (commit 246c598 + the settle-confirm-event-loop reconciler). The task's internal citation attributing that incident's mechanism to "the sibling gets cancelled after observing the first fill" does NOT hold up against the code or the incident's own audited root cause -- this should be corrected in the record. Race A/B are a DISTINCT, still-open exposure, capable of an even worse outcome (reversed, not just naked) than the incident they've been informally attributed to.

PROPOSED FIX (see ticket folder proposal_for_review.md for full detail): (1) PRIMARY -- tag TP/SL with a native IBKR ocaGroup/ocaType=1 in place_child_orders (ibkr_client.py), making sibling-cancel-on-fill an atomic broker guarantee instead of a software race; no parentId involved, so Finding 1's historical problem cannot recur. Single-file, no interface/signature change. (2) REQUIRED companion -- fast-detect + immediately-flatten backstop for a residual double-fill (the current silent "UNRECOGNIZED FILL" ERROR-log-only path is itself a live-real-money-system defect regardless of how unlikely Race A becomes). (3) SEPARATE finding, Race B -- recommend the idle-tick-reconciler-owned "verify legs actually left the book (or already produced a genuine fill) before submitting a fresh closing order" pattern, reusing the architecture the codebase just built for exactly this class of problem (settle-confirm-event-loop) rather than a same-tick cancel+submit race; flagged for likely HUMAN AUTHORIZATION given its proximity to the just-shipped (07-20) killswitch-pending-exit-guard fix on the same state machine.

Severity classification: MEDIUM/HIGH (structural logic shift; touches live real-money order construction and fill-handling). Proceeding to Impact-Reviewer per workflow (no fast-track).

[2026-07-21 19:40] | protective-leg-fill-race_07212026_1851 | TICKET-IMPACT-REVIEWER | Fix A and Fix B pass the Interface Rule / Base Class Rule / Refactor Veto (single-file, no signature changes, not a multi-component refactor) but neither is a clean APPROVE as literally worded: Fix A's ocaGroup-from-parent_order_id derivation collides with a live, already-shipped heal path that hardcodes parent_order_id=0, and Fix B's "trigger _check_naked_position" is a no-op given _active_trade_id is already cleared by the time the stale-id fill arrives -- both need a named correction before implementation, still within my approval authority. Fix C is correctly flagged for separation; I independently concur it requires Mandatory Human Authorization and must not be bundled into this ticket.

Full reasoning below (source re-verified directly against the actual current files, not inherited from the Auditor's line numbers):

BLAST RADIUS (Fix A -- ibkr_client.py::place_child_orders, ~1572-1641):
- Interface Rule: NOT triggered. Confirmed by direct read of src/live_execution/interfaces/execution_interface.py -- the abstract place_child_orders(self, symbol, parent_order_id, action, quantity, tp_price, sl_price) signature is untouched. adapters/ibkr_execution.py::place_child_orders (line 219) is a pure pass-through to manager.place_child_orders with the same kwargs -- no edit needed. adapters/simulated_execution.py::place_child_orders (line 332) is a wholly separate implementation (registers _RestingOrder entries) that already achieves same-bar TP/SL mutual exclusivity via a documented "SL wins (pessimistic)" bar-level mechanism (simulated_execution.py:13,603-604,636) -- confirmed it does not reference ocaGroup/ocaType anywhere and needs no change, matching the proposal's claim.
- Base Class Rule: NOT triggered. ibkr_client.py's place_child_orders is a concrete leaf method on IBKRConnectionManager, not a base class; ExecutionClient (the actual ABC/interface in this architecture) is untouched.
- Refactor Veto: NOT triggered. Single function, single file.
- CORRECTNESS FINDING (must-fix before implementation, HIGH): the proposal's "generate a per-trade-unique ocaGroup string derived from parent_order_id -- no new parameter needed" is unsafe as literally written because parent_order_id is NOT reliably unique in the current code. Proof: _verify_and_heal_protective_legs (live_trader.py ~2492-2630), which is shared by BOTH startup recovery (_recover_inherited_position) AND the hourly housekeeping heal sweep, calls place_child_orders(..., parent_order_id=0, ...) with a LITERAL HARDCODED 0 (live_trader.py:2590, comment "# no parent -- standalone") every time it re-places a missing TP/SL leg. If ocaGroup is built from this value verbatim, every heal-placed bracket across every symbol/process/heal-event over the fleet's life gets the IDENTICAL ocaGroup string. Any two such heals open at the same time (plausible: a shared-outage heal across several of the 5 sibling symbol processes, or the same symbol healing twice across restarts) would be linked by IBKR into ONE OCA group, so a fill on one instrument's healed TP could cause the broker to cancel an UNRELATED instrument's SL -- a cross-symbol naked-position event, i.e. exactly the class of failure this ticket exists to close, now caused by the fix. Note for the record: the proposal describes unprotected-leg-verification_07082026_0315 as "pending, operator-approved-but-unimplemented" -- that is incorrect; per the code's own docstring ("operator decision 2026-07-08, ticket unprotected-leg-verification_07082026_0315") this heal path is ALREADY LIVE and already wired into both startup recovery and hourly housekeeping, so Fix A must account for it now, not defer it as a future coordination note. Remediation stays single-function/single-file: mint uniqueness independently inside place_child_orders itself (e.g. symbol + parent_order_id + a uuid4/reqId suffix) instead of trusting the caller-supplied parent_order_id alone.
- SECONDARY FINDING (verify, not blocking, MEDIUM): whether IBKR's ocaGroup matching is enforced per-API-client-connection or account-wide across the 5 sibling client-id sessions is not stated in IBKR's public TWS API docs as far as I could find (checked interactivebrokers.github.io/tws-api/oca.html -- confirms ocaType=1 = "cancel all remaining orders with block" but is silent on cross-client scope). IBKR's own sample code derives the OCA group name by concatenating a prefix with the order id specifically for uniqueness, consistent with (not proof of) string-equality group matching that is not client-scoped. Note the codebase's OWN existing comment for a different primitive, cancel_orders_by_ids (ibkr_execution.py:292-296): "Per-instance client ids mean openTrades() exposes only this instance's own orders, so cross-child interference is structurally impossible" -- that guarantee is about each client's LOCAL ib_insync cache visibility and should NOT be assumed to extend to server-side ocaGroup matching, which is a different mechanism. This is exactly why the Fix-A remediation above (independently-unique ocaGroup, not parent_order_id-derived) matters regardless of how this question resolves -- it closes both the proven parent_order_id=0 collision AND this unverified cross-client-scope question at once. Recommend confirming on a paper account (two client sessions, deliberately identical ocaGroup string) before going live.
- LADDER/OCA-GROUPING FINDING (answering the specific question asked, informational): grouping all N TP-ladder legs + 1 SL leg into ONE ocaGroup/ocaType=1 does not introduce a NEW problem -- it preserves today's existing behavior. Confirmed by reading _on_standard_execution_event (live_trader.py:6363-6472): is_tp_fill already matches on ANY id in _tp_order_ids (any single rung), and on that match the code unconditionally calls the bulk cancel_open_orders(symbol=...) (cancelling the SL AND every other TP rung) and then _reset_position_state() (which clears _active_trade_id, disabling _check_naked_position's very first guard) -- regardless of whether that rung was the full position or a partial tier. So a single non-final-rung fill ALREADY causes software to believe the whole position closed and cancels the rest, today, with no OCA involved. Native OCA reproduces the same end state atomically at the exchange instead of via a race-prone Python cancel -- same risk profile, not worse. I'd flag the underlying issue (a partial ladder-rung fill leaves real broker-side contracts open while software believes it is flat and disarms the kill switch) as a genuine, SEPARATE, pre-existing defect worth its own follow-up ticket, but it is unchanged by Fix A/B and not a reason to block this one. One IBKR-level nuance I could not fully verify from source or public docs: ocaType=1's "with block" semantics are traditionally tied to all-or-none quantity guarantees, and the TP-ladder rungs + SL leg are NOT equal-quantity siblings -- recommend the Auditor verify this specific unequal-quantity combination on a paper account as a rollout condition for any symbol using tiered TPs.

BLAST RADIUS (Fix B -- live_trader.py, _reset_position_state ~1271-1297 + _on_standard_execution_event ~6363-6490):
- Interface Rule: NOT triggered. New private instance attribute + a new branch inside an existing function; no signature changes anywhere.
- Base Class Rule: NOT triggered. Confirmed class LiveTrader (live_trader.py:326) has zero subclasses anywhere in src/ -- it is a leaf class, not a widely-inherited base/utility.
- Refactor Veto: NOT triggered. Confined to live_trader.py; at most touches _reset_position_state, _on_standard_execution_event, and (per my correctness finding below) likely needs a small helper extracted near _check_naked_position -- all within the SAME class/file, not a cross-component rewrite.
- CORRECTNESS FINDING (must-fix before implementation, HIGH -- as described this would not achieve its stated purpose in the one scenario that matters): "trigger the SAME flatten machinery _check_naked_position uses" cannot literally mean calling _check_naked_position() itself. By the time the second (stale-id) fill event reaches the new branch, _reset_position_state() has already run from the FIRST fill and set self._active_trade_id = None (live_trader.py:1292). _check_naked_position's very first line is "if self._active_trade_id is None: return" (live_trader.py:6248-6249) -- so invoking it as-is would silently no-op, reproducing the exact "silent drop, no correction" defect this fix exists to eliminate, except now paired with a CRITICAL log + Telegram alert that falsely implies the book was flattened. The proposal's own line "this does not change _check_naked_position's own gating (still requires _active_trade_id)" shows awareness of the gate without explaining how the new caller satisfies or bypasses it. Needs an explicit resolution before implementation: either (i) extract _check_naked_position's flatten steps (cancel-all / market-close / ledger-close / telegram) into a shared un-gated helper both callers use, or (ii) have _recently_closed_leg_ids retain the trade id (not just order ids) so _active_trade_id can be deliberately restored before the call. Either remediation stays single-file.
- CORRECTNESS FINDING (real but conditional, MEDIUM): _check_naked_position has exactly one call site in the whole codebase -- the idle main-loop heartbeat tick (_event_loop, live_trader.py:5923) -- and it uses the non-cached self.exec_client.get_position(...) (-> get_cl_position -> self.ensure_connected() -> self.ib.connect() if disconnected). That is precisely the primitive this codebase elsewhere goes out of its way to avoid calling from inside an ib_insync event callback (see _check_time_barrier's own docstring: "may therefore only SUBMIT orders... and RECORD intent", and the get_cached_position vs get_position distinction documented directly on the ExecutionClient interface, which exists so hourly housekeeping does not deadlock the loop). Confirmed _on_standard_execution_event -- where Fix B's new trigger lives -- is wired via ib.orderStatusEvent += self._on_order_status (ibkr_execution.py:45), i.e. it runs inside the SAME already-running ib_insync event loop as bar-update callbacks. In the common (connected) case this is harmless (ensure_connected is a cheap no-op, ib.positions() is a sync cache read), but if the connection happens to be down at the exact moment of the stale-id fill -- not implausible, since the ticket's own motivating scenario is gappy/thin overnight markets, a period also correlated with connectivity flakiness -- invoking this flatten path would call self.ib.connect() from inside the already-running loop, risking the same "This event loop is already running" failure class this codebase has built substantial machinery elsewhere to avoid. Recommend whatever helper Fix B calls uses get_cached_position (the callback-safe primitive), mirroring the existing hourly-housekeeping precedent, not get_position.
- Housekeeping note: confirmed the legacy ibkr_client.py::place_bracket_order (with parentId children, ~1360-1496) is NOT reachable from the live trading path -- live_trader.py:5420 calls exec_client.place_bracket_order without tp_price/sl_price kwargs, which the adapter (ibkr_execution.py:189-217) routes to entry-only place_entry_order, not the full-bracket parentId method. So place_child_orders (Fix A's target) is confirmed to be the ONLY live order-children code path; no parallel path is left inconsistent by Fix A.

FIX C: I independently concur it must be split out and requires Mandatory Human Authorization -- not because the Auditor asked cautiously, but on the merits after reading the actual code. _check_time_barrier (live_trader.py:1651-1786) and _reconcile_pending_position_state (live_trader.py:1788-1971) together implement the exact submit-and-defer / idle-reconciler state machine that received killswitch-pending-exit-guard_07202026_1805 one day before this ticket opened -- confirmed by reading its extensive in-line documentation of BINDING CONDITION 1/2/3, the A1-A4 bounded-retry-budget contract, and the explicit "this is the ONLY permitted catch, never book/re-arm on an unconfirmed value" invariants threaded through both functions. A change here depends on the FULL cross-function sequencing and the idle-vs-callback execution-context distinction the whole file is built around -- "one file" undersells the real blast radius for this specific pair of functions given how load-bearing and fresh the machinery is. This warrants the Mandatory Human-Authorization Guardrail even though it may not strictly trip a literal multi-file Refactor Veto; per the workflow I am not autonomously approving it regardless of the justification's quality, and I agree it should not be bundled into this ticket.

DECISION: Fix A -- REQUEST REVISION (blast-radius clean: passes Interface/Base-Class/Refactor-Veto, approved to proceed within this ticket, but must NOT derive ocaGroup from parent_order_id alone; requires an independently-unique group-name construction before implementation, per the proven parent_order_id=0 collision in the live heal path). Fix B -- REQUEST REVISION (also blast-radius clean, but must resolve the _active_trade_id-gate no-op and use get_cached_position instead of get_position in whatever the new branch calls, before implementation). Fix C -- REQUEST HUMAN AUTHORIZATION, must be split into its own ticket, must not be bundled here. This ticket should not be marked complete/implemented until Fix A and Fix B's named corrections are incorporated into the plan.
