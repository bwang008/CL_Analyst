[2026-07-09 12:39] | recovery-barsheld-wallclock_07092026_1239 | TICKET-AUDITOR(inline) | Root cause: two restart-recovery sites in src/live_execution/live_trader.py convert WALL-CLOCK elapsed time into bar counts via int(delta_minutes / bar_dur), but market gaps (weekend ~49h, daily 1h halt, holidays) contain zero bars — (1) position recovery :1964-1977 overestimates _position_bars_held (Friday entry recovered Sunday computes ~52 "bars" vs true ~3 → instant spurious TIME_BARRIER close at Sunday open, since fleet max_hold_bars are 18-24); (2) _seed_restart_cooldown :1745-1753 overestimates bars_elapsed → cooldown over-aged → re-entry gate releases earlier than backtest parity allows. Normal (non-restart) operation counts real received bars (:1645) and is correct.
[2026-07-09 12:39] | recovery-barsheld-wallclock_07092026_1239 | TICKET-AUDITOR(inline) | Severity: HIGH (silently closes a live position early and breaks backtest-live parity; 2026-07-08 A/B showed weekend-carried positions are on average worth holding). NOT a recent regression (recovery estimator predates this week's exit-trigger work) but touches live execution → Impact-Reviewer gate mandatory, no fast track.
[2026-07-09 12:39] | recovery-barsheld-wallclock_07092026_1239 | TICKET-AUDITOR(inline) | Proposed fix: add one private helper LiveTrader._bars_since(ts) -> Optional[int] returning the count of BRAIN-stream bars strictly after ts from the rolling df matching self._bar_size (rolling_df_1h for "1h", rolling_df_5m for "5m"; None/empty df -> None). Site (1): replace the delta_minutes estimate with _bars_since(entry_bar_time); on None keep the existing conservative default (0). Site (2): replace delta_min/bar_dur with _bars_since(close_time); on None stay inert (existing behavior). Gap-immune by construction (counts actual bars, same philosophy as the flatten-bar gap detection); if entry predates the seeded window the count is a lower bound, which errs toward HOLDING (never toward a spurious close). No signature, base-class, or config changes; two localized call-site edits + one helper in one file.
[2026-07-09 12:46] | recovery-barsheld-wallclock_07092026_1239 | TICKET-IMPACT-REVIEWER | APPROVED with binding conditions C1-C5 (full review in impact_review.md): both sites and steady-state semantics verified off-by-none against :1639-1645, warm-start seeds rolling dfs (>=4320 1h bars enforced) before recovery at :945, no Interface/Base-Class/Refactor-Veto triggers (one file, private helper, no LiveTrader subclasses), hourly-only instances improve (site 1 currently never estimates when rolling_df_5m is None), human authorization for touching live execution on record; conditions: None+loud-log for non-{5m,1h} bar sizes (2h/4h resample would over-count 2-4x), no-raise exception safety, strict-greater counting with bars_elapsed-1 seeding untouched, re-pin test_restart_cooldown_recovery.py fixtures and retire test_execution_parity.py::TestRecoveryBarsHeld (pins the old wall-clock math), minimal diff.
