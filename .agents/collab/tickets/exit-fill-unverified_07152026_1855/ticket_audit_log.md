# Ticket Audit Log — exit-fill-unverified_07152026_1855

[2026-07-15 19:10] | exit-fill-unverified_07152026_1855 | TICKET-AUDITOR | Verified the reported TIME BARRIER exit path in src/live_execution/live_trader.py:1679-1725 against the live incident log — CONFIRMED fire-and-forget: cancel_open_orders (1679) then close_position (1682) whose return value is only scraped for an orderId (1701/1719), never checked for a fill, then telemetry.close_position with a fabricated exit_price=current_price (1708-1714) and _reset_position_state (1724).

[2026-07-15 19:10] | exit-fill-unverified_07152026_1855 | TICKET-AUDITOR | CONFIRMED both safety nets are disarmed by the exit path itself: kill switch _check_naked_position early-returns on `_active_trade_id is None` (5776) AND on `_sl_order_id is not None` (5782), both nulled by _reset_position_state (1219/1222); housekeeping takes the deliberately detect-only UNTRACKED branch (2757-2775) because the ledger row was already marked CLOSED. `grep -ci "KILL SWITCH" reports/fleet/fleet_20260714.log` = 0 — never fired.

[2026-07-15 19:10] | exit-fill-unverified_07152026_1855 | TICKET-AUDITOR | CORRECTED the Ticket-Manager's `buffer=0.00` hypothesis: NG tick_size=0.001 (src/core/instrument_master.py:151), so ibkr_client.py:781 `buf = 2 * inst.tick_size` = 0.002 — the buffer WAS applied; "0.00" is a `%.2f` log-format artifact at ibkr_client.py:788-790. Real proximate trigger = the marketable limit is priced off a STALE bar close and placed tif='GTC' with no re-price/timeout, so it rests forever when the market moves away.

[2026-07-15 19:10] | exit-fill-unverified_07152026_1855 | TICKET-AUDITOR | Surveyed all exit call sites: rollover (3606-3628) and kill switch (5809-5869) share the same cancel->close->reset shape but use exit_mode="market" (fills near-certainly, lower probability, same defect class); TP/SL fill callback (5972-5981) is correctly fill-driven. Late fills of an abandoned exit order are dropped as "UNRECOGNIZED FILL ... ignoring" (5991-5999), so the fabricated ledger price is never corrected.

[2026-07-15 19:10] | exit-fill-unverified_07152026_1855 | TICKET-AUDITOR | Established NOT a recent regression via bounded history: `git log -n 5` + `git blame -L 1676,1725` — the fire-and-forget shape dates to 85c08ded (2026-02-27); exit_price fabrication added 96af1e95 (2026-05-16); 128dc202 (2026-06-13) only threaded symbol= through the adapter refactor. ~4.5-month latent defect.

[2026-07-15 19:10] | exit-fill-unverified_07152026_1855 | TICKET-AUDITOR | Proposed a localized fix (no refactor): confirm the exit fill on broker truth before cancelling protective legs / booking the ledger / resetting state, re-arm on non-fill within a bounded window, and arm the kill switch on broker truth. Severity HIGH, blast radius = ALL FIVE live children (every fleet config uses exit_mode marketable_limit), human-authorization required. Reported to Ticket-Manager; going idle.

[2026-07-15 19:20] | exit-fill-unverified_07152026_1855 | TICKET-IMPACT-REVIEWER | INDEPENDENTLY VERIFIED the root cause and 10/11 Auditor claims from source: fire-and-forget confirmed (ibkr_client.py:824 `return self.ib.placeOrder(...)` returns immediately, :822 `order.tif="GTC"`, no fill wait); kill switch guard :5776 confirmed (runs every 5min via _HEARTBEAT_CYCLES=60 x _POLL_INTERVAL=5.0 — would have caught the naked position ~16:05 instead of never); housekeeping UNTRACKED detect-only :2755 confirmed; blast radius CONFIRMED = all 5 live children carry live.exit_mode="marketable_limit"; incident evidence CONFIRMED (KILL SWITCH grep=0, NG TIME BARRIER 16:00:05, naked event 16:15:01, CL same path 09:00:05 filled on luck).

[2026-07-15 19:20] | exit-fill-unverified_07152026_1855 | TICKET-IMPACT-REVIEWER | STRENGTHENED the Auditor's "ledger corruption is not self-healing" claim with the mechanism: _HOUSEKEEPING_OVERWRITE_REASONS = ("CLOSED_OOB","CLOSED_OOB_UNRECOVERED") at live_trader.py:196 — "TIME_BARRIER" is absent, so repair_closed_position (:2730) can NEVER overwrite the fabricated exit_price. Corruption is permanent by construction, not by accident.

[2026-07-15 19:20] | exit-fill-unverified_07152026_1855 | TICKET-IMPACT-REVIEWER | REFUTED the Auditor's load-bearing OCA premise: `ocaType`/`ocaGroup` appear NOWHERE in src/. place_child_orders (ibkr_client.py:1610-1634) places TP and SL as standalone orders explicitly WITHOUT parentId and WITHOUT any OCA group; mutual cancellation is software-side — live_trader.py:5953 comment reads "Software OCA: cancel the remaining resting protective order(s)". Orders 65/66 do NOT carry ocaType=3. Fix item 1 rests on a group that does not exist.

[2026-07-15 19:20] | exit-fill-unverified_07152026_1855 | TICKET-IMPACT-REVIEWER | RISK FOUND (severe, fix item 1): reversing :1679/:1682 to leave the SL resting during an unconfirmed exit, with NO native OCA, creates a DOUBLE-FILL / POSITION-REVERSAL hazard — on a +1 long both the exit limit and the stop are SELL 1; if both fill the account goes net -1 naked SHORT, and the SL fill routes to the is_sl_fill branch (:5943-5981) -> _reset_position_state -> bot believes FLAT while short. This reproduces the reconnect-false-flat-oob catastrophe class ($296k naked short). The Auditor's own design DEPENDS on OCA for atomicity; without it, item 1 trades a bounded naked window for an unbounded-loss reversal.

[2026-07-15 19:20] | exit-fill-unverified_07152026_1855 | TICKET-IMPACT-REVIEWER | BLAST RADIUS of the OCA route = Interface Rule + Base Class Rule + REFACTOR VETO all triggered: building the group would touch ibkr_client.place_child_orders + close_cl_position, ExecutionInterface.close_position signature (:187), ibkr_execution (:355), simulated_execution (:529, must model OCA), 3 live_trader call sites (time barrier/rollover/kill switch), and the now-redundant software-OCA path (:5953) — >=5 components. Compounded: all 5 configs run strategy exit_mode="TIERED" (tiered scale-out => _tp_order_ids is a LIST), so ocaType=3 reduce-with-overfill-protection has non-obvious resize/cancel semantics across TP tiers = live trading-economics risk, not a mechanical change.

[2026-07-15 19:20] | exit-fill-unverified_07152026_1855 | TICKET-IMPACT-REVIEWER | RISK FOUND (fix item 5): dropping the `_active_trade_id is None` guard at :5776 turns the 5-minute kill switch into a broker-truth reconciler that AUTO-FLATTENS UNTRACKED positions — reversing the deliberate OPERATOR-AUTHORIZED decision recorded at :2748-2755 ("UNTRACKED ... stays human-only", operator-authorized 2026-07-08, ticket unprotected-leg-verification_07082026_0315). While flat it would flatten ANY broker position in that symbol, including one a HUMAN just took over in TWS — exactly what happened in this incident at ~16:29. Also lowest marginal value: if items 2/3/4 work the position never becomes untracked. Operator policy call, not the Auditor's.

[2026-07-15 19:20] | exit-fill-unverified_07152026_1855 | TICKET-IMPACT-REVIEWER | SCOPING GAP: Auditor claims ONE test needs fake-fidelity repair; actual surface = 9 test files call _check_time_barrier, 6 of them stub close_position (test_cooldown, test_hourly_order_housekeeping, test_oob_entry_state_recovery, test_reconnect_false_flat_recovery, test_live_trader_bugs, test_exit_reason_and_fill_routing). A bare MagicMock exec_client returns a TRUTHY MagicMock from get_position_settled -> confirm-gate reads "still holding" -> reset never runs -> those tests fail. ~6x undercount creates precisely the pressure toward the FORBIDDEN "widen the assertion" shortcut. Precedent for the correct repair already exists at test_exit_reason_and_fill_routing.py:186 (get_position_settled.return_value = 0).

[2026-07-15 19:20] | exit-fill-unverified_07152026_1855 | TICKET-IMPACT-REVIEWER | PARITY CLEARED: Auditor's parity claim verified — the "CL stays bit-identical" comment (:2952-2955) governs round_to_tick on bracket children only. Independently strengthened: simulated_execution.close_position sets _position=0 SYNCHRONOUSLY (:568) and cancels its own resting orders (:565), so the confirm-gate is a no-op in the sim and cancel/close ordering is already irrelevant there => live/backtest parity cannot regress from items 2-4.

[2026-07-15 19:20] | exit-fill-unverified_07152026_1855 | TICKET-IMPACT-REVIEWER | VERDICT: REJECTED AS SUBMITTED + HUMAN AUTHORIZATION REQUIRED for two severable parts. Items 2/3/4 (confirm-gate reusing _confirm_settled_position :1856, bounded re-arm reusing _verify_and_heal_protective_legs :2071, fill-price-or-NULL per the :2306 rule) are LOCALIZED to live_trader.py, change no signature, touch no base class, honor all hard constraints => APPROVABLE once resubmitted WITHOUT the :1679/:1682 order reversal and WITHOUT the :5776 guard removal. Demanded safer fix + escalation sent to Ticket-Manager; going idle.

## Veto loop — iteration 1 (Impact-Reviewer REJECTED items 1 & 5)

[2026-07-15 19:48] | exit-fill-unverified_07152026_1855 | TICKET-AUDITOR | RETRACTION — my OCA premise was FALSE and I accept the reconciliation. Verified from source: `grep -rn "ocaType|ocaGroup|oca_group|oca_type" src/ --include=*.py` returns ZERO matches in every file; place_child_orders (ibkr_client.py:1610-1634) places TP as standalone LMT and SL as standalone STP with no parentId and no group; ibkr_client.py:1590 states "OCA behavior (cancel the other leg on fill) is handled in software by LiveTrader._on_order_status"; live_trader.py:645 "software-side OCA (no parentId linkage)" and :3020 "(standalone orders, software OCA active)". Reconciliation CONFIRMED: the ocaType=3 in the incident log is the broker-echoed ib_insync Order repr (orders 65/66 render as generic `Order(...)` from the openTrades echo), NOT set by us — order 71, our own locally-constructed object, renders as `LimitOrder(...)` with no ocaType at all; and no ocaGroup name appears in any repr, so ocaType binds nothing regardless. Item 1 WITHDRAWN — without atomicity it trades a bounded naked window for an unbounded-loss position reversal.

[2026-07-15 19:48] | exit-fill-unverified_07152026_1855 | TICKET-AUDITOR | Item 5 WITHDRAWN — dropping the :5776 guard would reverse the operator-authorized "UNTRACKED stays human-only" decision at :2748-2755 and could auto-flatten a position a human had just taken over in TWS. Accepted that item 3 re-arms the kill switch for free by KEEPING _active_trade_id set (:5776 passes) while _sl_order_id stays None (:5782 passes), making item 5 unnecessary.

[2026-07-15 19:48] | exit-fill-unverified_07152026_1855 | TICKET-AUDITOR | Verified the Reviewer's ledger finding at source: _HOUSEKEEPING_OVERWRITE_REASONS = ("CLOSED_OOB","CLOSED_OOB_UNRECOVERED") (:196) with the gate at :2712 skipping every other close_reason, so repair_closed_position (telemetry.py:1192, called :2730) can NEVER overwrite a TIME_BARRIER row — corruption permanent by construction. CONCLUSION REACHED THAT DIFFERS FROM THE OBVIOUS ONE: the constant must NOT be widened. Post-fix TIME_BARRIER rows carry PROVEN fill prices and belong exactly where they are, in the never-overwrite bucket next to TP_HIT/SL_HIT whose comment (:193-195) reads "carry real prices and are NEVER touched". Widening it to repair history would permit overwriting future truthful rows. Historical repair (trade_64, GC trade_27) = separate operator-gated one-off, not a constant change.

[2026-07-15 19:48] | exit-fill-unverified_07152026_1855 | TICKET-AUDITOR | Enumerated the true test surface (my prior count of 1 was ~6x under): 9 files call _check_time_barrier; 4 stub exec_client.close_position and so reach the exit branch and WILL fail on a bare MagicMock (auto-attr get_position_settled returns a truthy Mock != 0 -> gate reads "still holding" -> reset never runs): test_cooldown.py:182+210, test_exit_reason_and_fill_routing.py:165, test_live_trader_bugs.py:150, test_oob_entry_state_recovery.py:612. Fake-fidelity repair follows the precedent the reconnect-false-flat-oob ticket already set at test_exit_reason_and_fill_routing.py:186 (`get_position_settled.return_value = 0  # settled CONFIRMS flat`). Assertions stay unchanged — teaching a fake to model a broker interaction is NOT loosening a test.

[2026-07-15 19:48] | exit-fill-unverified_07152026_1855 | TICKET-AUDITOR | Confirmed the revised fix needs ZERO signature/interface/base-class changes: _confirm_settled_position (:1856), _verify_and_heal_protective_legs (:2071), cancel_orders_by_ids and get_executions all already exist on ExecutionInterface AND both adapters (ibkr_execution + simulated_execution). Also confirmed _check_time_barrier ALREADY calls _confirm_settled_position at :1592 in its own OOB branch, so the settled-read main-thread contract (ibkr_client.py:665-666) is already satisfied in this exact call context.

[2026-07-15 19:48] | exit-fill-unverified_07152026_1855 | TICKET-AUDITOR | Submitted revised proposal scoped to approved items 2/3/4 with cancel->close ordering PRESERVED, adding a re-confirm-after-cancel step the Reviewer's "cancel before re-arm" rule implies but does not cover (a fill racing the cancel would otherwise leave a re-armed STP resting on a flat book — a stop that OPENS a naked reversal if triggered). Severity remains HIGH, human-authorization required, all 5 live children in blast radius.

[2026-07-15 19:34] | exit-fill-unverified_07152026_1855 | TICKET-IMPACT-REVIEWER | ITERATION 2 — VERIFIED the Auditor's OCA retraction from source rather than accepting it: ibkr_client.py:1590-1591 "OCA behavior (cancel the other leg on fill) is handled in software by LiveTrader._on_order_status"; live_trader.py:645 "TP/SL order tracking for software-side OCA (no parentId linkage)"; :3020 "(standalone orders, software OCA active)". Retraction is grounded in the design's own words. Item 1 and item 5 withdrawals ACCEPTED. Auditor's kill-switch-re-arms-for-free reasoning independently confirmed (:5776 passes with _active_trade_id set, :5782 passes with _sl_order_id None).

[2026-07-15 19:34] | exit-fill-unverified_07152026_1855 | TICKET-IMPACT-REVIEWER | ADJUDICATION (a) — RACE IS REAL, MITIGATION CORRECT, NOT SUFFICIENT. Verified cancel_orders_by_ids (ibkr_execution.py:298-313) iterates openTrades() and calls ib.cancelOrder(trade.order) FIRE-AND-FORGET — it never waits for a terminal Cancelled state, and returns only the count of ids FOUND OPEN. Auditor's race confirmed: a filled order has already left openTrades() so the cancel returns 0 (silent no-op) and an un-re-confirmed re-arm would place an STP on a FLAT book = a stop that OPENS a naked reversal. Step (b) re-confirm correctly routes that to the flat branch. Addition APPROVED and the Auditor was right to flag it for adjudication rather than slip it in.

[2026-07-15 19:34] | exit-fill-unverified_07152026_1855 | TICKET-IMPACT-REVIEWER | RESIDUAL RACE FOUND (the branch the Auditor did not trace) -> BINDING CONDITION 1: when cancel_orders_by_ids returns 1 the exit is only CANCEL-REQUESTED, not dead; the subsequent settled re-confirm is a ~100ms reqPositionsAsync round-trip during which the exit can still fill if the fill beats the cancel at the exchange (the codebase already documents fast fills winning these races at ibkr_client.py:1583-1588). Sequence settled=1 -> cancel(71)=1 -> re-confirm=1 (snapshot pre-dates fill) -> re-arm SL+TP -> 71 fills -> resting STP/LMT on a flat book = the same reversal hazard, bounded only by the next-bar OOB branch (:1587-1620) i.e. up to 1 HOUR on NG's 1H brain. DEMANDED: never re-arm while an exit that can still fill is live — cancel returns 0 => not open => re-confirm routes it, no re-arm; returns 1 => re-scan get_open_trades until _exit_oid has LEFT the book, take the settled read STRICTLY AFTER (ordering is load-bearing), and only then re-arm; if still open, defer the re-arm, stay tracked, return False (the 5-min kill switch covers the gap, and flattening a position past its time barrier is the desired end state anyway). Localized, existing primitives, no new machinery.

[2026-07-15 19:34] | exit-fill-unverified_07152026_1855 | TICKET-IMPACT-REVIEWER | ADJUDICATION (b) — AUDITOR IS RIGHT; I ACCEPT AND WITHDRAW THE IMPLIED REMEDY. Verified :193-196 ("the ONLY close reasons whose exit_price/close_reason may be overwritten by a proven broker fill. TP_HIT/SL_HIT rows carry real prices and are NEVER touched") and :2712-2713 (skips everything outside the tuple). My finding was DIAGNOSTIC (why the incident row is permanently corrupt), never a prescription to widen the constant — and the Auditor's inference is the better one: post-fix TIME_BARRIER rows carry PROVEN prices, placing them exactly in the never-touched bucket beside TP_HIT/SL_HIT. Widening :196 would license housekeeping to overwrite TRUTHFUL rows, converting a correctly-scoped guard into a NEW corruption vector. DO NOT TOUCH :196. The defect is the fabricating write at :1713; fill-price-or-NULL per the :2305-2313 precedent is the right remedy. trade_64 / GC trade_27 repair = operator-gated one-off script, out of scope (agent cannot write the live DB). Principled pushback, upheld.

[2026-07-15 19:34] | exit-fill-unverified_07152026_1855 | TICKET-IMPACT-REVIEWER | TEST SURFACE ACCEPTED as fake-fidelity repair, NOT loosening. Verified all 4 stub sites reach the exit branch: test_cooldown.py:182,210 (MagicMock()), test_exit_reason_and_fill_routing.py:165, test_live_trader_bugs.py:150 (mock_trade), test_oob_entry_state_recovery.py:612 (SimpleNamespace). Adding get_position_settled.return_value=0 + a matching get_executions stub teaches the fake to model a fill that REALLY HAPPENED — the fake gains fidelity while the assertion keeps its teeth; _reset_position_state.assert_called_once_with(reason="TIME_BARRIER") (:179) survives unchanged => SL-flavored cooldown parity preserved. Must-stay-green verified: test_reconnect_false_flat_recovery.py:250/262 assert close_position/cancel_open_orders/_reset_position_state NOT called — the new None branch mirrors that fail-closed invariant exactly. BINDING CONDITION 2: the regression test must ALSO pin the race branch (settled=1, then cancel_orders_by_ids -> 0 because already filled => flat branch books the EXECUTION's price and does NOT re-arm), else the addition rots untested.

[2026-07-15 19:34] | exit-fill-unverified_07152026_1855 | TICKET-IMPACT-REVIEWER | BLAST RADIUS RE-MAPPED, ALL THREE VETOES CLEAR: revised fix is confined to live_trader.py; _confirm_settled_position (:1856), _verify_and_heal_protective_legs (:2071), cancel_orders_by_ids (interface :154 + both adapters), get_executions (interface :172 + both adapters) ALL pre-exist. Zero signature changes => Interface Rule NOT triggered. Zero base-class/ibkr_client edits => Base Class Rule NOT triggered. One component => Refactor Veto NOT triggered. Contract note verified (_check_time_barrier already calls _confirm_settled_position at :1592, same call context). Parity intact (sim close_position sets _position=0 synchronously :568 and cancels its own orders :565 => gate is a no-op in sim). Hard constraints clean: None branch fails closed LOUDLY (no swallow), missing orderId = hard failure (no silent null default), bounded by bars/attempts (no blind sleeps), no assertion widened. close_cl_position:825 `return None` verified => the trade-is-None hard-failure branch is grounded.

[2026-07-15 19:34] | exit-fill-unverified_07152026_1855 | TICKET-IMPACT-REVIEWER | VERDICT: **APPROVED** subject to 2 BINDING CONDITIONS (C1: gate the re-arm on the exit order being confirmed GONE from openTrades, never merely cancel-requested, settled read strictly after; C2: regression test must pin the cancel-returns-0 race branch). Out-of-scope list accepted (rollover :3606, native OCA build, untracked-auto-flatten, %.2f log format). Both prior rejections were absorbed with verified evidence and one correct counter-argument against my own finding — the fix is now localized, fail-closed, and reuses existing primitives. Reported to Ticket-Manager; going idle.
