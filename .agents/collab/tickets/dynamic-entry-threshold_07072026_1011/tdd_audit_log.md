# TDD Audit Log — dynamic-entry-threshold_07072026_1011

[2026-07-07T10:15:00Z] | dynamic-entry-threshold_07072026_1011 | TDD-MANAGER | Read blueprint + tdd-manager/tester/coder workflows; mapped the 3 consumers of _PARAM_RANGES["entry_threshold"] in agent/strategy_optimizer.py (_suggest_side_params L960, non-tiered objective loop L1003, warm-start _extract_warm_start_params L793/816). Confirmed branch=training-update.
[2026-07-07T18:07:06Z] | dynamic-entry-threshold_07072026_1011 | TDD-TESTER | Wrote tests/test_dynamic_entry_threshold.py (19 tests, 8 classes) covering _entry_threshold_bounds quantile/firing identities (uniform+skewed+clipped-normal), the firing<->quantile inversion (permissive=lower threshold), degenerate/compressed distributions (SI-model 100%-firing bug), NaN dropna, 3-way warm-start _snap_to_grid consistency, module constants FIRING_FRAC_MIN/MAX, and end-to-end Optuna integration asserting sampled entry_threshold_long/short fall inside per-side dynamic bounds. Verified RED: all 19 fail with AttributeError on missing so.FIRING_FRAC_MIN / so._entry_threshold_bounds. Independently confirmed the integration synthetic OHLCV/predictions run clean through a real BacktestEngine (trades generated) so red is solely the missing implementation, not a bad fixture.
[2026-07-07T11:06:00Z] | dynamic-entry-threshold_07072026_1011 | TDD-TESTER | Authored tests/test_dynamic_entry_threshold.py (18 tests across 8 classes): _entry_threshold_bounds quantile/firing identities on uniform+skewed+clipped-normal, firing<->quantile inversion, degenerate/tiny-jitter, compressed SI-bug (no always-on), NaN drop, warm-start dynamic-grid snapping via _snap_to_grid, and make_objective Optuna integration asserting per-side sampled thresholds stay in dynamic bounds.
[2026-07-07T11:40:00Z] | dynamic-entry-threshold_07072026_1011 | TDD-CODER | Implemented blueprint in agent/strategy_optimizer.py: FIRING_FRAC_MIN=0.05/FIRING_FRAC_MAX=0.45 constants; _entry_threshold_bounds(prob_series,f_min,f_max)->(low,high,step) with dropna, degenerate/compressed widening, [0,1] clamp, min step 1e-3; _compute_entry_thr_bounds() precompute per side (prob_Buy/prob_Sell) with static fallback; make_objective gained f_min/f_max/entry_thr_bounds kwargs and applies dynamic bounds to entry_threshold in _suggest_side_params AND the non-tiered objective loop (all other params stay on _PARAM_RANGES); _extract_warm_start_params gained entry_thr_bounds and snaps entry_threshold to the dynamic grid; both run_optimization & run_hybrid_optimization precompute bounds post-holdout-slice and share them across objective+warm-start; CLI --firing-frac-min/max threaded through. Added per-side [ENTRY-THR] log line. Kept static _PARAM_RANGES["entry_threshold"] as fallback.
[2026-07-07T11:41:00Z] | dynamic-entry-threshold_07072026_1011 | TDD-CODER | Fixed genuine _snap_to_grid boundary bug (round(snapped,10) nudged a boundary snap ~1e-11 outside the many-decimal dynamic [low,high], which would make enqueue_trial reject the warm-start baseline) by re-clamping after rounding; static-grid consumers unaffected (clean 2-decimal bounds). Did NOT modify the strict-locked test file.
[2026-07-07T11:45:00Z] | dynamic-entry-threshold_07072026_1011 | TDD-MANAGER | GREEN gate: tests/test_dynamic_entry_threshold.py 19/19 pass; full fast suite 10 failed / 1678 passed (was 29/1659 at RED) — the 10 remaining are the identical pre-existing unrelated ES01B config/instrument sentinel failures, zero new regressions. Optimizer+warm-start+seed-determinism suite (43 tests) green. Working tree left uncommitted for review; wrote tdd_result.md.
