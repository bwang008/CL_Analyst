[2026-07-04 23:59] | es-baseline-zero-metrics_07042026 | TICKET-AUDITOR | Root-caused ES/all-non-CL baseline zero metrics to producer-side silent signal-lookup miss: gcp/vm_e2e_pipeline.py run_backtest (L272-273) and ensemble block (L897-898) read {SYM}_raw.parquet via raw pd.read_parquet without promoting the DateTime column to index (all new-symbol raw parquets ship int64 RangeIndex, unlike GCS CL_raw.parquet which has a DatetimeIndex), so backtest_engine.py L1206 pd.Timestamp(row.Index) yields 1970-era timestamps and L1221 prob lookups default to 0.0 -> 0 trades; reproduced locally (0 trades as-shipped vs 934 trades/,300 with index fixed); classified latent-since-ES-standup (read code from c5111ca1 2026-06-16, first zeroed run 20260702-0114 canary); confirmed top_pairs.json selection UNCONTAMINATED (unified_pair_optimizer parses batch_summary_optimized_*.md whose metrics batch_post_optimizer recomputes locally via load_ohlcv); enumerated blast radius (all ES/NG/NQ/ZC/GC/ZS/SI batches ALL_ZERO, CL healthy); proposed localized fix (normalize exec parquet index at load + crash-on-empty-lookup-overlap guard) with 3 options; severity MEDIUM.
[2026-07-04 23:18] | es-baseline-zero-metrics_07042026 | TICKET-IMPACT-REVIEWER | APPROVED Options A+B (with C workflow-doc addendum as follow-up): mapped blast radius — Option A is fully internal to gcp/vm_e2e_pipeline.py (run_backtest is not imported by any other module; only train_final_model/generate_oos_predictions/_sigmoid are imported externally; no signature/return-type changes, Interface Rule not triggered; load_ohlcv promotion is a no-op for the healthy CL_raw.parquet so CL batches are regression-safe); Option B triggers the Base Class Rule (BacktestEngine.run has ~25+ call sites across strategy_optimizer.py, optuna_lgbm_search_v2.py, batch flows, scripts, and 37 calls in tests/test_backtest_engine.py) but the Business Justification Exception is met (repo standing fail-loudly policy, localized alternatives A-alone and C-data-only explicitly considered, guard would have caught the bug at first ES canary; all test fixtures build signals with index=ohlcv.index so zero-intersection can never fire on legitimate callers, and optuna_lgbm_search_v2.py:381-390 wraps engine.run in except-Exception-return-0.0 so the Optuna hot loop cannot break); Refactor Veto NOT triggered (two small localized edits — 2 call-site substitutions + 1 additive guard — no component rewrite); approval conditions: (1) guard must live on the strategy-aware path and raise ONLY when prob lookups are NON-empty AND intersect ohlcv.index zero times (never raise on legitimately-empty thresholded legacy signal_sides or unresolvable prob columns), (2) implementer should note gcp/vm_e2e_pipeline.py:756 raw pd.read_parquet(data_path) remains un-normalized as the :902 ensemble fallback (practically dead since exec_data_path is schema-required; Option B guard covers it if ever misaligned).
