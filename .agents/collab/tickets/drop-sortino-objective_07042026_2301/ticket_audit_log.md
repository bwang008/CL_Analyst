# Ticket Audit Log — drop-sortino-objective_07042026_2301

[2026-07-04 23:55] | drop-sortino-objective_07042026_2301 | TICKET-AUDITOR | Completed read-only blast-radius audit of removing the Sortino objective pass: classified all 52 files with case-insensitive 'sortino' hits; found the objective list is purely code-side (gcp_deploy_optimizer.ps1:30 default "both" -> vm_post_optimize.sh:104 -> batch_post_optimizer.py "both" expansion), NOT in BatchSweepConfig (src/config/schemas.py:229) or any v2 manifest, so backward-compat req is satisfied by flipping two deploy-chain defaults to "sharpe"; identified scripts/compare_parity.py:34-44 REQUIRED_INDIVIDUAL as the ONLY hard-fail consumer of absent sortino artifacts (proposed split into required-sharpe + optional-legacy-sortino lists); confirmed unified_pair_optimizer.py, generate_ensemble_artifacts.py, analyze.py, dashboards, and all GCS upload/download paths tolerate missing sortino files via existence checks and globs; flagged run_sweep_batch.ps1:989-998 VM-sizing x8->x4, generate_batch_configs.py latent --objective=both no-op that starts working under sharpe, and 3 docs to update; recommended code-side constant (Option A) over any manifest objectives field, severity MEDIUM / risk LOW, seed-offset fix (_OBJECTIVE_SEED_OFFSETS) and 51099e1 report-parity fixes untouched, rollback = pass -Objective both (abstraction retained). Proposal sent to Ticket-Manager.

[2026-07-04 23:14] | drop-sortino-objective_07042026_2301 | TICKET-IMPACT-REVIEWER | APPROVED WITH CONDITIONS after independently verifying every load-bearing claim read-only: confirmed compare_parity.py:34-44/97-119/138-142 split is sound both directions (section-3 content checks already skip missing files via isfile+continue so present legacy sortino files stay content-checked, ref cross-check is warnings-only), confirmed no drift risk from the double default flip (gcp_deploy_optimizer.ps1:220 uploads vm_post_optimize.sh fresh every deploy and :293 always forwards --objective=$Objective explicitly, so the sh:104 default only governs manual VM runs), confirmed run_sweep_batch.ps1:1011-1023 passes no -Objective (manifests untouched, req 1 holds by construction), verified generate_batch_configs.py:189 latent both->exit-1 no-op and judged the newly-active sharpe output harmless (only consumers are passive download blocks gcp_deploy_optimizer.ps1:373-377 and run_sweep_batch.ps1:1103; gcp_monitor.ps1:264 documents *_opt.json is NOT auto-routed to configs/strategies; filenames objective-namespaced per generate_batch_configs.py:309), verified via repo-wide code sweep that no other code path hard-requires sortino artifacts (unified_pair_optimizer.py:15-17 warns+returns [], generate_ensemble_artifacts.py:299-305 skips, tests reference strategy_optimizer internals only), endorsed code-side-constant over a manifest field (optional field = banned silent default, required field = breaks req 1; objective is operational like zone/n-trials, and vm_post_optimize.sh:142 + Telegram :307 log it); rules check: Interface Rule NOT triggered (no signatures change), Base Class Rule NOT triggered (shared deploy scripts keep their parameter interface), Refactor Veto NOT triggered (default flips + constant-list split, no component rewritten). CONDITIONS: (1) doc updates MUST explicitly disclose that run-vector-cloud-batch inherits sharpe-only via the shared gcp_deploy_optimizer.ps1/vm_post_optimize.sh chain (run-vector-cloud-batch.md:59) and Manager must surface this to the user in the changelog; (2) also fix the stale x2-objectives comment at run_sweep_batch.ps1:989-990 alongside the sizing math; (3) merge gated on the stated validation plan (57-test suite + 14B canary + compare_parity PASS with sharpe-artifact byte-comparability via seed offset 0). Decision reported to Ticket-Manager.
