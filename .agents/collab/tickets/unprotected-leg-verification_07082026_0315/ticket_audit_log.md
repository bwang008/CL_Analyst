[2026-07-08 03:40 PT] | unprotected-leg-verification_07082026_0315 | TICKET-AUDITOR | Independently verified the silently-lost-SL hazard: traced ib_insync error/orderStatus callbacks (_on_ib_error handles only connectivity codes 10182/1100/1101/2103/2105/2104/2106 — NO order-rejection codes; _on_standard_execution_event acts ONLY on status=="Filled" with no Cancelled/Rejected/Inactive branch; _check_naked_position short-circuits at `if self._sl_order_id is not None: return` so a dead-but-tracked id is never revalidated). Confirmed fleet_health false-negative (checks DB sl_order_id presence only, no broker session) and the :15 _housekeeping_sweep naked check tests only "some STP exists" without cross-checking the ledger's expected sl_order_id/tp_order_id against resting ids. Designed deliverable A (post-place/modify orderStatus acknowledgement verification + async-rejection detection seam) and B (extend :15 sweep with expected-id-vs-resting cross-check emitting a housekeeping-* event). Sent RCA + design to Ticket-Manager.
[2026-07-08 04:15 PT] | unprotected-leg-verification_07082026_0315 | TICKET-AUDITOR | REVISED per operator redirect to AUTO-HEAL: designed a factored-out `_verify_and_heal_protective_legs` (extracted from _recover_inherited_position ~1898-2019: query get_open_trades, cross-check ledger tp/sl_order_id vs resting ids, and on a genuine miss re-place from ledger CURRENT prices via place_child_orders) called by the :15 _housekeeping_sweep, idempotent (fires only on a real gap), gated by _check_order_rate_limit, software-OCA-safe, LOUD health event on heal. Recommended hardening the trailing->ledger persist (~1503) to loud-on-failure since a stale sl_price makes a heal re-place the ORIGINAL SL. Evaluated operator's placement-race hypothesis against place_child_orders (ibkr_client.py 1483-1552): TP+SL are back-to-back synchronous placeOrder GTC standalones, software-OCA (no ocaGroup), no shared-transmit dependency -> NO placement-time drop race; NG SL was verified resting so it was lost via the async-cancel path, not placement; a 1s inter-leg delay would HURT (naked window after entry fill) and is a forbidden masking sleep. Sent revised design to Ticket-Manager.
[2026-07-08 04:35 PT] | unprotected-leg-verification_07082026_0315 | TICKET-IMPACT-REVIEWER | APPROVED WITH MANDATORY CONDITIONS. Independently verified blast radius: extract is a pure refactor of _recover_inherited_position 1898-2019 with byte-identical startup behavior (no signature/base-class change, single-component — no Refactor Veto). Confirmed get_open_trades reads ib_insync's IN-MEMORY openTrades() cache (ibkr_execution.py 355 / ibkr_client.py 628), NOT a live reqOpenOrders — this is a genuine false-missing vector (transiently empty/partial cache post-reconnect; is_connected()==True does not guarantee the cache was repopulated), and now it drives a WRITE (cancel+replace) not just an alert. Confirmed software-OCA exit cancel (5513) is a bulk symbol-scoped cancel_open_orders so sibling cleanup is id-independent, BUT is_tp_fill/is_sl_fill booking (5495-5502) IS id-dependent -> re-registration of _tp_order_ids/_sl_order_id is load-bearing (design does register, correct). Confirmed cancel_open_cl_orders (ibkr_client.py 624-640) cancels the GOOD TP too when re-placing both -> a single-missing-SL heal cancels+re-places a healthy TP = brief resting-TP gap + 2 orders toward the 10/60s halt. update_position_brackets COALESCEs initial_sl_price so heal-at-trailed-sl_price preserves the true original (no invariant broken); persist-hardening no-rollback is correct (broker SL is right, only ledger stale). place_child_orders routes through ensure_connected() -> flagged vs the sweep's A-2 local-cache-only invariant, but the sweep runs on the event-loop thread between ib.sleep() (same context the rollover check already uses placeOrder in), so a place is consistent IFF is_connected() is re-checked immediately before it. CONDITIONS: (1) require a confirm-missing recheck / post-reconnect cache-freshness guard before any cancel+replace so a stale-cache false-miss cannot churn good orders; (2) re-check is_connected() immediately pre-place and abandon on disconnect (no ensure_connected reconnect under _ledger_lock); (3) rate-limit gate must FAIL-CLOSED to LOUD cannot-heal (never place if _check_order_rate_limit/_emergency_halt trips) and idempotency gate must verify the newly-placed ids are actually found on the NEXT sweep to prevent re-fire loops; (4) prefer replace-only-the-missing-leg (leave a verified-resting TP untouched) to avoid cancelling healthy orders and halve halt pressure — cancel-both only when >1 leg missing; (5) wire the new housekeeping-protective-leg-healed kind into _ALLOWED_KINDS + test_emitted_kinds_stay_in_the_contract_set + SKILL.md triage. Sent verdict to Ticket-Manager.
